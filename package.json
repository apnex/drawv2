{
	"name": "draw",
	"version": "0.1.0",
	"description": "Strictly minimal grid-aligned diagramming tool — browser editor, server persistence, one-way Google Slides sync",
	"license": "MIT",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=18"
	},
	"scripts": {
		"start": "node server/server.js",
		"test": "node --test tests/*.test.js",
		"gate": "npm test && node tools/scan-writers.mjs && node tools/scan-claims.mjs docs/spec/COMMIT.md#7 docs/spec/COMMIT-DELETIONS.md",
		"gate:install": "printf '#!/bin/sh\\nnpm run gate\\n' > .git/hooks/pre-push && chmod +x .git/hooks/pre-push && echo 'pre-push hook installed'"
	},
	"dependencies": {
		"ws": "^8.21.0"
	}
}
