.git
.refs
secrets/
node_modules
diagrams
tests
*.md
docs/
# design/ moved under docs/ on 2026-09-03 and is covered by the line above. The bare entry is gone
# rather than kept "just in case": a rule naming a path that does not exist reads as protection and
# provides none, and the next reader cannot tell which of the two is load-bearing.
*.log
.gitignore
.dockerignore
Dockerfile
docker-compose.yml
# the local CLI context pointer must never be baked into the image (.gitignore
# does not affect the build context) — it would pin a stale dev diagram id
cli/.draw_context
