# draw — backlog

Defects, deferrals and known gaps. Every row carries an evidence citation and either the milestone that
closes it or a **revival trigger**. Explicit deferral is permitted; silence is not.

Seeded at CS1 from `docs/spec/COMMIT.md` §10. Rows B1–B9 were discovered during the commit-system design
and had nowhere else to live — this repo had one commit and no defect register.

At arc close, every row is either closed by its named milestone or carries a live trigger.

| # | Row | Evidence | Closes / Trigger |
|---|---|---|---|
| **B1** | **`POST /groups` admits a node to two groups.** `server/rest.js:38` calls `model.makeGroup(d.members)` directly; `model/model.mjs:206-212` performs no steal; `server/validate.js:150-153` checks member *existence* only. The "at most one group" rule exists **only** in the browser (`app/src/commands.js:157-175`). | `[V, all four sites read]` | **Closed CS1** — the steal rule moves into `plan()` with its own inverse and a multi-group-overlap test. |
| **B2** | **Adopt-local-content destroys a real diagram.** On a snapshot arriving after the user has drawn, the client adopts *whatever diagram the server answered with* and pushes its own content over the top. | `[V, app/src/sync.js:87-107; the push at :107]` | **CLOSED CS4** `[V, a6adb7d..HEAD]` — `create {name, doc}` → `install()` with a **server-minted** id; `doc.meta.id` is ignored, so a client can no longer name — and therefore no longer target — an existing diagram (I11). `push` is deleted outright. Pinned by `tests/resume.test.js` *"content drawn before the server answers lands in a NEW diagram, not over an existing one"* and, server-side, `tests/server.test.js` *"I11: `create {doc}` mints the id"*. |
| **B3** | **Server rejections are silently dropped; the browser diverges permanently.** A rejection is a bare string that only `console.warn`s; anything sent while the socket is closed is dropped with a `false` return and no queue. | `[V, app/src/sync.js:139; app/src/net.js:64-67]` | **CLOSED CS3** `[V, e9a90b1..HEAD]` — typed `error {code, txnId}` correlated to the request; a rejected optimistic apply now triggers `requestResync()`, and a change whose `from` is ahead of us repairs rather than guessing. Both found by `tests/convergence.test.js`. |
| **B4** | **A failed flush reschedules nothing.** `flush`'s catch logs and returns, leaving `entry.dirty = true` while `markDirty` already nulled the timer — recovery waits for the next edit or SIGTERM. On a backend where `renameSync` fails transiently, a session's final transaction **and its inverse** can sit in memory indefinitely. | `[V, server/store.js:322-324 vs :306]` | **CLOSED CS2** `[V, be44575..HEAD]` — the catch reschedules; `flushFailures` is counted per diagram, totalled by `Store.flushFailures()`, surfaced as `/health {status:'degraded', flushFailures}` and a `draw status` line. Negative-tested: an injected failing `writeDoc` recovers with no further edit. |
| **B5** | **`Store.init` fabricates success.** A file failing `validateDoc` is skipped with a `console.warn` (`server/store.js:125-128`); if the store empties, it reseeds (`:142`); `/health` then answers 200 with a plausible, complete, wrong store, and `Dockerfile:43-44` asserts HTTP 200 only. `diagrams/*.json` is gitignored — the 17 files are untracked. | `[V, server/store.js:120-142; .gitignore:4; 17 files counted]` | **Closed CS1** — D17 / **GR8** / **I15**. Permanently retires the data-disappearance class for every future schema change. |
| **B6** | **No `fsync` anywhere.** `writeFileSync` + `renameSync` without `fsync` can lose the last write on a machine kill. Unchanged by this arc, but "undo survives a restart" will be read as a stronger claim than the code makes. | `[V, exhaustive grep: 0 hits; server/store.js:319-320]` | **CLOSED H8.2** -- the revival trigger fired and is answered. `gcsFiles(bucket)` in `server/files.mjs` puts the store on GCS, where durability is the object store's rather than an absent `fsync`, so the question this row asks stops being ours. Raw `fetch` over four endpoints, not the SDK. An adapter and not a `gcsfuse` mount, because the mount emulates `rename` as copy-then-delete and so cannot give the no-splice property the seam promises -- an object store gives it natively on a single PUT. Writes are compare-and-swap on `ifGenerationMatch`, tracking the generation each object was last seen at, and `0` for a name never seen so a create cannot silently clobber. A 412 throws and deliberately does NOT re-read and retry: healing that race is precisely the overwrite the precondition exists to prevent. `BUCKET` selects it in `server/server.js`, explicitly rather than by sniffing the environment, and the absence of `BUCKET` leaves the filesystem default exactly as it was. Evidence is in two halves because neither half is sufficient. Eleven unit tests over a fake `fetch` cover what a real bucket makes hard to force -- pagination, a 412, a 503 storm, bounded retries, token caching, the injection branch both ways -- but they only prove the adapter matches MY MODEL of the API. `tools/gcs-probe.mjs` runs the four verbs and a full two-`Store` round trip against the live `gs://diagrams.apnex.io`, which is the only thing that can falsify the model; it is outside the gate because CI has no credentials and a silently-skipping test is worse than none. Two of my own errors are worth recording. The unit fake keyed on `/o?`, which the LIST url also contains, so it answered both calls with the upload body. And the probe's first namespace filtered on read without prefixing on write, so the seed landed at the bucket root, the cleanup assertion passed over an empty set, and it littered the real bucket -- the cleanup check now asserts it removed something. Finally, `metadataToken` turns `TypeError: fetch failed` into a named diagnosis, because off-GCP the real cause sits two `cause` levels down and an operator got undici internals for a one-line configuration mistake. |
| **B7** | **Preview writes to the shared Model.** `app/src/input.js:769-773` writes into the live model once per pointer-move frame; a remote change landing mid-gesture fights the live preview. ~~Mitigated by D12's defer rule, not fixed.~~ *(Corrected 2026-08-19: the mitigation did not exist — `Sync.deferInbound` was read and assigned nowhere, so this row was UNMITIGATED from CS3 until H3.2, not merely unfixed. **Mitigated for real as of H3.2 / B19**: `bindGestureDefer` connects the gesture FSM to the inbound queue in both directions, and a test drives a live `Input` through it.)* | `[V, app/src/input.js:768-773; exhaustive grep: `deferInbound` has exactly one occurrence, the read at app/src/sync.js:245]` | **Open. REVIVAL TRIGGER: the renderer-overlay arc.** The fix duplicates a geometry pipeline across four files (N7). The *mitigation* is not deferred — it is **B19**, closing H3. |
| **B8** | **Eviction is invisible at every surface.** The ring evicts at 100 records / 32 KiB; a bounded, designed loss no actor can perceive is not a bounded loss. | `[V, D23 caps; I14 reporting requirement]` | **CLOSED CS6** `[V, 6d6477f..HEAD]` — `evicted` persisted at CS2; `truncated` on `history`/`snapshot`/`sync`/`change` at CS3; and at CS6 the **floor**: eviction stops at the newest human-authored record, so an agent's batch cannot push the user's own last change out of the ring. Because eviction must be oldest-first (a hole corrupts every inverse above it), the ring GROWS past the soft cap rather than evicting around the floor — bounded by a hard ceiling at 4x, recorded as **X11**. Crossing it sets `evictedHuman`, surfaced in the browser as *"your oldest changes are no longer undoable"* and in `draw history` as a Dropped line. Pinned by four tests in `tests/history.test.js`. |
| **B9** | **The accountability record and the undo ring are the same object.** An undo ring must be bounded, destructive and redo-truncating; an accountability record must not be. D2's authority answer rests on attribution the ring can evict. | `[V, server/store.js:319 — the entire document is rewritten every debounce tick, so a 10k-row trail is ~600 KB per 200 ms]` | **Ruled CS2/CS3** — the ring stays bounded (D23, `[LOCKED]`); attribution is scoped to the ring via `actor`; the absence beyond it is declared as **N12**, and D2's justification no longer leans on attribution that outlives the ring. **The row stays open as N12's revival trigger: any requirement for a durable audit trail — compliance, a multi-tenant deployment, or an incident review needing history older than the ring.** |
| **B10** | **A put-based inverse does not restore intra-kind ordering, and draw order IS collection order.** `model.put` appends, so undoing a deletion moves the entity to the end of its collection; the renderer iterates `model.all(kind)` (`app/src/renderer.js:124-128`), so two overlapping same-kind entities can swap stacking across a delete+undo. **Pre-existing, not introduced by CS1** — the client's `applyEntry` used `model.put` identically. | `[V, app/src/renderer.js:124-128; model/model.mjs:67 put appends; git show HEAD~1:app/src/commands.js applyEntry]` | **Open. REVIVAL TRIGGER: a user reports stacking changing after undo, or explicit z-order becomes a feature.** The fix is an explicit order field, not an ordered restore. |
| **B11** | **`meta` was a websocket command the server stopped answering.** `case 'meta'` was deleted at CS3a when meta became an op inside `commit`, and nothing wired the client to the replacement. The browser kept sending `meta` from four call sites — rename, the Slides URL field, the pre-hydration URL replay, and the push binding — and every one was answered `unknown cmd: meta` and dropped. **Renaming a diagram in the browser did nothing for two milestones.** | `[V, server/protocol.js dispatch has no 'meta' case since a9f9ff0; app/src/sync.js sent it at 4 sites]` | **CLOSED CS5** — rename and deck-binding are CHANGES and go through the commit boundary as a `meta` op: undoable, broadcast, versioned. Found by `tests/spec.test.js` on its first run, which derives the wire vocabulary from the server's own dispatch instead of trusting the document. |
| **B12** | **The Slides binding had no writer.** `model/ops.mjs` documented `presentationId`/`pageId` as *"STATUS, written by the server after a push (see Store.bindSlides)"* — and `Store.bindSlides` did not exist. The binding was in fact written by the browser's `meta` message, which B11 had already made a no-op, so **no successful push has persisted where it landed since CS3a**. The next push after a restart re-targets `pages[0]`: a wrong-slide overwrite, not a missing feature. | `[V, exhaustive grep: 0 definitions of bindSlides; server/rest.js returned the report without persisting it]` | **CLOSED CS5** — `Store.bindSlides` written and called by the push handler, so the CLI's `draw push` binds too and the browser needs no round trip to record something it did not do. Status, not a change: no version bump, not undoable, survives a restart. |

## Audit 2026-08-19 — rows B13–B33

Seeded by a full-tree audit at the close of the CS arc. **The register vocabulary matters here: most of
these are not new work.** B13–B20 are *locked decisions and invariants that have silently gone false* — each
row names the D/I/GR row it violates. B21–B23 are the reason they were able to go false: three holes in the
net that was supposed to catch exactly this. Prioritisation and sequencing live in `docs/BOARD.md`.

| # | Row | Evidence | Closes / Trigger |
|---|---|---|---|
| **B13** | **A `$` in any entity name corrupts the diagram file.** `serialize` splices the log block in with `String.replace(regex, string)`, so `$&`, `` $` ``, `$'` and `$1` in the replacement are **expanded as replacement patterns**. `block` contains every log record, which contains entity `name` values, and `FIELDS.node.name` admits any string (`server/validate.js:51`). `$&` expands to the matched `"\n}"`, injecting a raw newline into a JSON string literal. The write **succeeds** and `dirty` is cleared (`server/store.js:343`), so the failure is silent until restart — at which point `parse` throws, the file is skipped, and if it is the only diagram `Store.init` **refuses to boot** (D17/GR8), turning a data bug into a boot failure. Violates **D18** (one module owns the on-disk format) and makes **GR8/I15** fire on a file the store itself corrupted. The same splice also silently drops the log if `body` does not end in `"\n}"`. | `[V, server/docfile.mjs:40; reproduced: a node named `a$&b` serialises to a file that fails JSON.parse — "Bad control character in string literal"]` | **CLOSED H1.1** — the splice is now a **slice**, not a `replace`: no pattern is interpreted and the position is computed rather than matched, which closes the `$`-expansion AND the silent log-drop when the body did not match `/\n\}$/`. Pinned by three tests in `tests/persist.test.js`, each **verified red first** — the round-trip over 7 replacement patterns, the empty-body log-drop, and the end-to-end restart proving the escalation (*"the store refused to boot on a file it corrupted itself"*). Format regression checked by re-serialising all **35** real files in `examples/` + `diagrams/` against the old implementation: **byte-identical 35/35**. |
| **B14** | **Arrow-key nudge and both Shift+arrow resizes throw and do nothing.** All three read `this.history.stack[this.history.index - 1]` — the pre-CS3 `History` shape. `Changes` has neither field, so this is `undefined[NaN]` → TypeError, thrown *before* the commit in every case. **D11 specified the replacement and it was built and tested** (`Changes.amend`, `app/src/changes.js:59-62`, `tests/boundary.test.js:88-131`) — no call site was rewired. README advertises all three gestures; W1's multi-cell node authoring gesture is among them. | `[V, app/src/input.js:1276, :1311, :1342 vs app/src/changes.js:36-45 — no `stack`, no `index`]` | **CLOSED H3.3** — all three rewired onto `Changes.amend`, which is what D11 specified at CS3 and what was written, tested and never called. The three near-identical coalesce blocks, `lastNudge`, `lastResize` and `NUDGE_COALESCE_MS` are gone (**-45 lines**). One property needed rescuing rather than porting: Input reset its own burst state on selection change so a burst never spanned one; `amend` keys only on label and time, so `Changes.flush()` was added and the seam moved into the window it belongs to. Five tests — three **verified red as `todo`** since H2.1, two more (the W1 node-span gesture, and the selection-change seam) **verified red against stashed pre-fix source**. |
| **B15** | **`durableVersion` over-reports durability whenever more than one commit is in flight.** It is derived from a boolean `dirty` with no flushed watermark on the entry: three commits inside one 200 ms debounce window make the ack for v3 report `durableVersion: 2` when **nothing** has flushed. The client prunes its persisted outbox on this number (**D30**), so it discards work that is still only in memory — and the high-frequency case is exactly the one the ws debounce exists for. On the undo/redo path (`:245`) it is unconditionally wrong: `store.undo` always calls `markDirty`, so `dirty` is always true. Violates **D13**'s contract and undermines **D30**. | `[V, server/protocol.js:105 and :245; no flushed-version field exists on an entry — server/store.js:335-359]` | **CLOSED H1.3** — `flush()` records `entry.flushedVersion` where the write actually lands, and `Store.durableVersion(id)` / `Store.log(id)` make the rule expressible once. All **3** spellings and all **10** hand-walked `store.diagrams.get(...)` sites are gone; `install()` seeds the watermark from the file it loaded (a created diagram is durable at nothing until flushed). `tools/scan-writers.mjs` now fails on any reach into `store.diagrams` outside `store.js`, and was **verified to bite by injection** (X13). Pinned by two tests **verified red first**: three commits in one debounce window advance nothing, and a failed write holds the watermark back until a retry lands. |
| **B16** | **`POST .../commit` does not take the transaction vocabulary, and `expect` is ignored on every REST forward write.** `commitWrite` branches on `mutation.action` and treats the whole body as ONE op, so the documented ws shape `{ops:[…]}` answers `422 unknown op 'undefined'`. Two consequences: **multi-op transactions are unreachable over REST** — an agent must issue N round trips that another writer can interleave, which is the exact hazard `undo {to}` exists to mitigate — and `body.expect` is never forwarded to `store.commit`, so an agent believing it has CAS protection on a forward write has none. `server/rest.js:257` states forward-write `expect` is "optional so a curl one-liner still works"; it is not optional, it is discarded. Violates **GR10** (README:166 and the route comment both assert the opposite of the running wire), **D14**, and X1's premise. | `[V, server/rest.js:81-84 and :91; exhaustive grep of server/rest.js: `expect` appears only on the undo/redo path; tests/locks.test.js:133 and tests/server.test.js:565 both post the legacy shape]` | **CLOSED H4** — both halves. `expect` rides the **`X-Draw-Expect` header** and reaches the transaction (a forward write's body IS an entity payload, so a reserved key there would collide with validation; the header is uniform across `/commit`, POST, PATCH and DELETE and mirrors the lock token — undo/redo keep the body form because their body is control, one statable rule). A stale precondition answers **409** and writes nothing (I1). And `/commit` now takes `{ops, label?}`, so a multi-op transaction is one change, one version bump, and no window for another writer to interleave; the legacy `{action,kind,entity}` shape is **gone, not aliased** (X1) and answers 400 `ops-required`, with the high-level verbs building ops directly. |
| **B17** | **The REST change broadcast omits `undoTop`.** A browser viewing a diagram an agent is writing over REST receives a `change` with no `undoTop`, so its *"undo all N changes by `<actor>`"* affordance goes stale or clears. `truncatedHuman` is included and its companion is not, which reads as an omission rather than a decision. **D21** therefore holds on one transport only. The two transports also disagree on `expect` policy (ws requires it on undo only when you did not author the top record and never on redo; REST always requires both) and on where `to` is type-checked. | `[V, server/rest.js:284-285 vs server/protocol.js:247; `undoTop` occurs 4× in protocol.js, 0× in rest.js]` | **CLOSED H4a** — `reversalBody()` in `server/protocol.js`, used by both transports. `changeBody` could not serve: undo/redo append no record (D3), so there is no Change to project, which is why the payload was hand-built twice and drifted. Unifying also surfaced **B39** — the ws was waiving D14's mandatory `expect` — fixed in the same pass. |
| **B18** | **Server-Locked read-only leaks three mutation paths.** `onDown` checks `readOnly` at `:213`, but the run-mode branch (`:181-193`, which reaches `labels.openContent()` → `commands.setContentValue` → `history.commit`) and the text-tool branch (`:196-205`, which commits a text box at `:932`) both run **above** it; in `onKeyDown` the guard is at `:1440` but `t` arms the text tool at `:1394`. `labeledit.js` has no `readOnly` awareness of its own. The change applies **locally** and `Sync.submit` drops it (`app/src/sync.js:62`) — permanent silent divergence with no resync trigger. Violates SCOPE decision 5 (single writer) and **I16** (no submitted request discarded without a user-visible notice). | `[V, app/src/input.js:181-193, :196-205, :1394 — all above their guards at :213 and :1440; app/src/sync.js:62]` | **CLOSED H3.1** — fixed with **B37**, since they are one defect in two directions. The gate is now **semantic rather than positional**: an explicit boundary comment marks inspection above / mutation below, `t` carries its own `!readOnly` clause, and run mode — which straddles the boundary — is split so firing a `draw:action` still works while locked (it commits nothing) while opening the inline editor does not. Six tests, three **verified red first**, plus a regression guard proving all three paths still work UNLOCKED, so the fix is a gate and not a wall. |
| **B19** | **D12's defer rule was never wired — `Sync.deferInbound` is read and never assigned.** `sync.js:245` guards on `this.deferInbound && this.deferInbound()`; exhaustive grep finds exactly one occurrence of the name, that read. Consequently `this.deferred` (`:42`) is never populated and `releaseDeferred()` (`:291-296`) is never called. **This is B7's stated mitigation**, so B7 has been unmitigated — not merely unfixed — since CS3, and GR6's fault (ii) (a change delivered while `input.mode !== null` targeting the dragged entity) exercises a queue that does not exist. | `[V, exhaustive grep: `deferInbound` = 1 occurrence, app/src/sync.js:245; `releaseDeferred` = 1 definition, 0 callers]` | **CLOSED H3.2** — `bindGestureDefer(input, sync)` in `app/src/sync.js`, called from the composition root. Made a NAMED unit rather than two loose property assignments, because a loose assignment at the composition root is exactly what went missing for two milestones: half of it cannot now be written without the other half being visibly absent, and a test can call it. `onUp` became a wrapper so the release fires on **every** exit path — it returns early per gesture mode, so a hook at the bottom fired for a marquee and nothing else — and it runs in a `finally`, since a throwing commit handler stranding the queue forever would be worse than the bug it replaced. `isGesturing()` also stopped returning `null` from a predicate. Pinned by two tests driving a real `Input` through the binding; the pre-existing `sync.test.js` cases always passed because they set `deferInbound` by hand — the mechanism was never broken, only unconnected. |
| **B20** | **The GR9 post-condition is inside the write's try/catch, so a structural breach is reported as an I/O failure — once, then never again.** Order in `flush()` is write → `dirty = false` → assert → catch. If the invariant throws: the write already succeeded and `dirty` is already false; the catch increments `flushFailures` and logs *"flush failed"*, which is not what happened; `/health` reports `degraded` **permanently** (the counter never decrements); and the rescheduled retry returns immediately at `:337` because `dirty` is false, so the breach is never re-checked. An invariant violation and a transient rename failure share a counter and a recovery path. | `[V, server/store.js:341-358]` | **CLOSED H1.4** — the assert moved out of the try/catch and the two failures were separated end to end: `Store.invariantFailures()` beside `flushFailures()`, `/health` answering `corrupt` (this process mis-minted; no retry helps) vs `degraded` (the disk said no; a retry may), a distinct greppable message naming the offending seqs, and a `Log:` line in `draw status` — the operator surface that would otherwise still have shown nothing. The document is still written: the breach is in the log's accounting, not the user's data, so refusing to persist would trade real work for a bookkeeping bug. Pinned by two tests in `tests/persist.test.js`, both **verified red first** — one asserting the breach is not counted as I/O, one asserting it is re-checked on every subsequent write rather than reported once and forgotten. |
| **B21** | **GR1 has no self-check: `tests/gate.test.js` does not exist.** GR1 states that *"`tests/gate.test.js` asserts that hook exists and is executable"*. It does not exist. `.git/hooks/pre-push` **is** installed in this working copy, but a hook is a local untracked artifact — a fresh clone is completely ungated — and there is no CI (`.github/` absent). So the guardrail that gates all thirteen others runs only when a human types `npm run gate`. A third instance of **X13**'s lesson: *a guardrail must be shown to bite before it is counted.* | `[V, ls tests/ — no gate.test.js; ls .github — absent; .git/hooks/pre-push present but untracked]` | **CLOSED H2.2** — `tests/gate.test.js` exists, with 6 injected probes each verified to bite (scanner dropped from the gate, `&&` weakened to `;`, installer hardcoding a path, a neutered hook, a non-executable hook, a missing register). Writing it found the defect the row understated: `gate:install` hardcoded `.git/hooks/pre-push` while a **global `core.hooksPath`** redirected git's lookup to `~/.config/git-hooks`, so the installed file was never executed — **the gate had never run on a single push**, and the file's presence made the repo look gated. Replaced by `tools/install-hook.sh`, which resolves git's real hook path and pins a repo-local `core.hooksPath` rather than writing into a directory shared with the machine's other repositories. **CLOSED H2.3** — ruled: with **no git remote** (one clone, one machine, one developer) CI has nowhere to run, so the local hook plus the gate self-check IS the whole gate. Recorded as deviation **X14**, time-boxed: the owner has stated drawv2 will be pushed to a remote after further board items, and on that push X14 expires — add CI and flip the hook check from warn to assert. Residual risk accepted explicitly: a future clone that skips `gate:install` is unprotected and only a console warning says so. |
| **B22** | **GR5's browser-inverse half was never written: `tests/diff-inverse.test.js` does not exist.** `tests/diff-plan.test.js` (the planner oracle) ships; the inverse differential does not. GR5 explicitly warned that a builder-only oracle covers ~15 of 23 transaction shapes and **misses 8** — the link-`closed` toggle, star/chain, route creation, node retype, endpoint re-attach and both resize paths. Those 8 are precisely the inline commit sites that still carry vestigial `before:` keys, contradicting `app/src/commands.js:9-11`. The opportunity GR5 names as *"destroyed permanently once the old code is gone"* has passed. | `[V, ls tests/ — no diff-inverse.test.js; GR5 names it and enumerates the 8 sites: app/src/input.js:519, :546, :651, :876, :907, :972, :1321, :1352]` | **CLOSED H2.5 — RETIRED**, recorded as deviation **X15**. Not merely unbuilt: **inapplicable**. A differential needs two implementations of one function, and the browser inverse builders were removed outright rather than replaced — CS3 moved inverse derivation to the server planner and the browser now sends forward intent only (`app/src/changes.js:21`; `toOp` drops `before`; exhaustive grep finds no client inverse derivation). The oracle was never frozen either — `tests/fixtures/` holds `plan-reference.mjs` alone — so GR5's escape hatch was not taken and the opportunity is gone exactly as GR5 warned. The risk is covered better by property tests: I3/I4's round-trip proves the inverse is CORRECT, where a differential would only have proven it matched code that could itself have been wrong. Revival trigger: any commit that replaces the server planner's inverse derivation with a second implementation. |
| **B23** | **No test constructs any client UI class.** Zero hits for `new Input`, `new Renderer`, `new Palette`, `new LabelEditor`, `new DataView`, `new Readout` across `tests/`. `input.js` alone is 1,609 lines, ~40 methods, 11 gesture modes and 23 of the 29 commit sites in the app — entirely uncovered. **This is the systemic root cause of B14, B18 and B19**: three defects in wiring that no suite could see. The four largest functions in the tree (`onKeyDown` 223 lines, `onUp` 167, `onDown` 160, `onMove` 91) are all in this file. | `[V, exhaustive grep across tests/: 0 constructions of any of the six client classes]` | **CLOSED H2.1** — `tests/fixtures/client-harness.mjs`: ~150 lines of stubs, **no new dependency**, because `input.js`'s entire global surface is four lines and nine of fifteen client modules touch no DOM. Constructs a real `Model`/`Changes`/`Selection`/`Input` and captures `Changes.onCommit`. 13 characterization tests in `tests/input.test.js`, all asserting **only** at the commit boundary — enforced by a new `scan-writers` rule (proven to bite) so the net survives H6 untouched instead of becoming a tax on it. Found two defects on its first run: `Input`'s default `readout` null-object was not total (fixed here — a null object that throws is a trap) and **B37**. B14 ×3 and B37 ×1 are marked `todo` with their rows, never written around. **Extended at H2.6, rescoped.** The row said *Renderer, Selection, LabelEditor*, which was stale — `Selection` is the best-covered class in the client (15 constructions). The question that mattered was not *which classes lack tests* but *what does H6 move that nothing would catch breaking*, and walking H6's six units against the net gave one answer: **`overlay.js`**. Five units end in a commit; the affordance surface — `hovered`, `armed`, `datumEl`, the crosshair, ~112 lines — draws and commits nothing, so H6.3 could relocate all of it green. `tests/affordance.test.js` covers it by BEHAVIOUR: hover and arming through the `renderer.setState` collaborator contract (which `overlay.js` will still honour), handles and the datum by what is DRAWN in a layer. Five H6.3-shaped regressions verified caught. `Renderer` and `Palette` remain uncovered deliberately — coverage there buys H6 nothing and would be work for its own sake. |
| **B24** | **`readJson` never settles above the 1 MB cap, and mis-splits multibyte bodies.** On the trip `req.destroy()` is called, but `'end'` will not fire and `'error'` is not guaranteed — so the promise never settles, the `await` in `handleWrite` hangs forever holding the closure alive, and the `.catch()` at `rest.js:146` never runs because nothing rejects. Separately, `buf += c` UTF-8-decodes each chunk independently, so a multibyte character split across a chunk boundary becomes U+FFFD — a CJK node name near a 64 KiB boundary lands mangled. The cap is on decoded characters, not bytes. | `[V, server/rest.js:23-30]` | **CLOSED H1.5** (promoted from H4 by the axiom review — A7's mandate is that no actor is permanently blocked). Every terminal event now settles exactly once, `'close'` as the backstop; bytes accumulate as `Buffer`s and are decoded ONCE; the cap is a byte cap. Oversize answers **413** with a code and the limit rather than a destroyed socket, because a dropped connection cannot be told from a network failure and so is not actionable (A7). The 413 path sets `Connection: close` and destroys after flush — a first cut that merely stopped reading desynchronised the keep-alive pipeline and hung the NEXT request, i.e. reproduced B24 one request downstream; the multibyte test caught it by timing out. Both tests **verified red against the old reader**: `threw:ECONNRESET` instead of 413, and `'\uFFFD\uFFFD\uFFFD計図'` for the split character. |
| **B25** | **`create {doc}` lets a client seed a `meta.version` the log does not share.** `cleanMeta` preserves a non-negative integer `meta.version` verbatim (`server/store.js:38`) and `create`'s candidate resets only `id` and `name`, while `install` supplies a default `new Log(0)`. So `create {doc:{meta:{version:999}}}` installs a model whose `meta.version` is 999 against a log at 0, and the returned snapshot reports two different numbers for one document (`snapshotBody.version` = 0 from the log, `snapshotBody.doc.meta.version` = 999 from the model). **D6**'s one-source-one-mirror contract is broken until the first commit re-stamps. **I11** is careful that a client cannot mint the *id*; it should say the same of the *version*. | `[V, server/store.js:38, :190, :162]` | **CLOSED H4a** — validated AS IT ARRIVED, then installed at 0. The order is the fix: a MALFORMED version is still rejected at the boundary (D17 — refused, never repaired silently) while a well-formed but client-chosen one is ignored (I11). Forcing 0 before validation collapsed both into "silently accept anything" — caught by an existing test, which is the trust boundary defending itself. |
| **B26** | **Dead and duplicate server surface, including a second unversioned writer of `meta.name`.** `Store.patchMeta` (`server/store.js:269-278`) has **zero callers anywhere** — its ws caller was deleted at CS3, `handleSlidesPush` migrated to `bindSlides` — and it is a second, unversioned, non-undoable write path for `meta.name`: exactly the duplicate writer the transaction model exists to eliminate. Alongside it: four imports with zero uses (`NODE_EXT`, `ZONE_EXT`, `validateMutation`, `groupAfterRemoval`, `plan` — `:10, :12, :13, :14`; `COMMIT-DELETIONS.md:44` scheduled the first pair for deletion and the function went while the import stayed), and `const tmp` at `:340` assigned and never read, which makes `flush` look like it still owns the atomic write. Also: `Store` exposes no `log(id)` accessor, so `store.diagrams.get(id)?.log` is hand-walked at **10 sites** across `protocol.js` and `rest.js` — which is what made **B15** possible in two files at once. | `[V, exhaustive grep: 0 callers of patchMeta; server/store.js:10, :12, :13, :14, :269-278, :340; 10 `diagrams.get(` sites outside store.js]` | **CLOSED H5** — `patchMeta`, the four unused imports and the dead `tmp` are gone; the `Store` accessor landed with B15. Detected, not noticed: `tools/scan-dead.mjs` (GR15) now counts every export against its production consumers and is in the gate. |
| **B27** | **Bounds are validated per field, never per derived extent.** A zone with `x: 960, w: 1920` passes and reaches 2880 — three canvas-widths off-surface. A node with `x: 960, span:{cols:64,rows:64}` passes (the comment at `validate.js:21-22` acknowledges the anchor check does not cover the span). A content region's `at:[64,64]` with `cols:64` is admitted with no check against its owning node's `span`, so a region can sit entirely outside its own node. None is a memory-safety issue — all caps are finite — but they admit documents the client cannot author and the renderer must cope with. Relatedly `NODE_EXT`/`ZONE_EXT` have **no server-side reader at all**, so `model/surface.mjs` is a shared source with only one side sharing. | `[V, server/validate.js:15, :21-25, :29-46, :75-77]` | **Open. TRIGGER: a hand-edited or agent-authored document renders off-surface, or the first non-browser authoring client.** The client clamps tighter (±900/±480) so no in-app gesture can reach these. |
| **B28** | **`kernel/adapt.mjs` is not in the production path, and the content-region renderer is cloned.** `docToSchema` has exactly one consumer in the tree — `tests/span.test.js` — yet `design/widgets/preview.html:166` and `tests/span.test.js:310-321` both describe *"the production path (document → docToSchema → kernel)"*. The live client never constructs a schema: `app/src/renderer.js` builds DOM directly and carries a **line-for-line clone** of `kernel/renderer.mjs`'s content-region code (same socket-union math, same wrap algorithm, same `hexColor` regex, same action sanitiser) plus the socket-grid loop and the panel `rx` rule. Two copies, no shared source, and the kernel copy is the one the tests call "production". | `[V, exhaustive grep: no app/ module imports schema.js; app/src/renderer.js:27, :31-62, :161, :168-171 ≡ kernel/renderer.mjs:24, :34-66, :71-77, :104-107]` | **CLOSED by PROMOTION** — neither delete nor relabel. The owner's intent settled it: the kernel renderer exists so a NON-CLIENT can download the SVG image, and that capability had never been wired up. `schema.js` was not dead — it was the bridge to a production that did not exist. Built as `GET /d/<id>.svg`; the adapter moved to `kernel/adapt.mjs` (it produces a kernel schema from plain document data, so hosting it there adds no dependency edge and the browser never called it anyway). `KERNEL_CSS`, flagged as a dead export by the same audit, was the other half of the same gap. The two renderers stay, now for a stated reason: the client maintains live addressable elements for a person editing; the kernel produces a complete document for everyone else. **Still open and moved to a fresh row: the ~11 lines of text-layout maths genuinely shared between them.** |
| **B29** | **Waypoint-awareness is inconsistent across seven client surfaces.** `Model.endpointOf` exists so that "node or waypoint" is one concept, and the link renderer and delete cascade use it. These do not: `refreshHandles` resolves endpoints with `get('node')` so a waypoint-ended link gets **no re-plug handles** (`input.js:1178-1179`) and positions handles on the straight `src→dst` line ignoring `via` (`:1182-1188`); re-plug can only target a node (`:715, :900`); **Ctrl+A omits waypoints** though they are SELECTABLE and marquee-pickable (`:1557-1561` vs `:958`); the readout prints `? ↔ ?` for waypoint routes (`readout.js:125-127`); and the data view both skips waypoint-ended links and measures straight-line distance ignoring `via`, so **the displayed length is wrong for every routed link** (`dataview.js:64-67`). | `[V, all seven sites read]` | **CLOSED H3.5** — the fix was a missing NAME, not seven missing patches. `Model.pathOf(link)` resolves a ROUTE (ordered anchors) to a PATH (ordered coordinates) and is now the **only** place that crossing happens; the four hand-rolled copies are gone, two of which were wrong — the data view measured `dist(src,dst)` ignoring every bend, and re-plug handles sat on the straight src→dst line. It admits a bare position as an anchor, exactly as the kernel's `resolveRoute` does, which is what lets the live preview (final anchor = the cursor) share the one resolver instead of being a fifth copy. Endpoint resolution moved to `endpointOf` at every site that is not legitimately node-only. Preceded by **T0/T1** (`1c0865e`): the taxonomy ratified in `HIERARCHY.md` §0 and the word `anchor` freed from five meanings to one. Nine tests — four on `pathOf` in `tests/model.test.js`, four on the display surfaces in the new `tests/display.test.js`, all **verified red first**. Handle placement is display-only, outside the commit-boundary net, and is covered by the same `pathOf`. |
| **B30** | **Cloning a routed link silently straightens it.** `cloneClosure` rebuilds a cloned link as `{id, src, dst}`, dropping `via` and `closed`; waypoints are also excluded from the clone seed set (`:375-376`) and from `duplicateSelection` (`:439`). Ctrl+drag or Ctrl+D over a multi-hop route produces straight links with no warning and no readout flash. | `[V, app/src/input.js:390, :375-376, :439]` | **CLOSED H3.4** — `cloneClosure` gained one per-kind `cloneEntity`, waypoints joined the seed set (and `duplicateSelection`'s filter), `via` is remapped through the id map, and `closed` is carried. Any via waypoint not already in the clone set is **pulled into the closure**, because pointing the copy at the ORIGINAL bends would make two links share a waypoint — which the validator forbids outright (a waypoint belongs to at most one link, in one role). The trap avoided: a waypoint admits only `{id, x, y}`, and the old seed loop stamped a name on everything it cloned, so a naive fix would have applied locally then been refused on the wire. Four tests, three **verified red against stashed pre-fix source**; the fourth runs every emitted entity through the real `validateEntity` and guards the fix's own trap rather than the original defect. |
| **B31** | **Five documented paths do not exist.** `README.md:248` cites `.refs/` (absent); `kernel/README.md` lists `view.mjs` and `fixtures.mjs` as kernel modules while `design/README.md` says `view.mjs` moved to `design/` — neither `kernel/view.mjs` nor `design/view.mjs` nor `design/shot.mjs` exists; `design/README.md` cites `docs/spec/CLEANLINESS.md` (absent); `engine/index.mjs:1` and `engine/policy.mjs:6` cite `docs/history/PRISMV2-DESIGN.md` (absent). Separately the I/GR pinning-test column names four suites: `tests/persist.test.js` + `tests/history.test.js` and `tests/persist.test.js` were renamed into `tests/persist.test.js` (benign, but the citation is now wrong), and `tests/gate.test.js`/`tests/diff-inverse.test.js` genuinely do not exist (**B21**, **B22**). | `[V, exhaustive: all five paths checked absent; ls tests/ vs COMMIT.md §4/§5 pinning column]` | **CLOSED H5** — and it was more than five. `tools/scan-docrefs.mjs` (GR15) found **38** unresolved references across 725 in docs, then a further **9 inside CODE COMMENTS** that the first cut of the scanner never looked at — including two `see docs/history/PRISMV2-DESIGN.md` pointers sending a reader to a file deleted long ago, which is the worst form of drift because it directs with authority. Genuine drift repointed: `app/src/schema.js`→`kernel/adapt.mjs` (my own move, one commit old), `client/src/renderer.js`, the renamed test suites, and HIERARCHY §7's planned DRC checker which had in fact landed as `kernel/grc.mjs`. Provenance citations and historical records are allow-listed with stated reasons — a deletion table naming the dead is the record working, not rotting. |
| **B32** | **An agent cannot create or delete a diagram.** `POST /api/v1/diagrams` reaches `handleWrite` with no id → `404 unknown diagram: undefined`; `DELETE /api/v1/diagrams/:id` hits the lock gate → 423, and with a lock → `404 unknown collection: undefined`. Diagram lifecycle is websocket-only, which is why the CLI is read-plus-push. For an API whose stated purpose is server-side agentic control, this is a notable asymmetry — though it is also the boundary that keeps the destructive verb away from a shell one-liner (see X12). | `[V, server/rest.js:207, :301; lifecycle lives at server/protocol.js:194, :291]` | FIXED -- ruled 2026-08-23. `DELETE /api/v1/diagrams/<id>` exists. WRITE access, not owner, and deliberately the same gate `store.remove` already applied to the websocket `delete`: the ruling's own logic sets the threshold -- whoever may empty a diagram may remove it, and emptying needs write -- and two transports disagreeing about who may delete would be the divergence this codebase keeps finding. Refused with 423 while another controller holds the lock, mirroring the websocket rule, except for the holder itself, since an agent removing a diagram it is driving is the ordinary case. The lock is released with the diagram rather than pinning one that no longer exists. `Hub#retarget` moves viewers onto a survivor built for THEIR principal -- a snapshot is per-principal, so a broadcast could not do it -- and the store reseeds rather than going empty. The director's stated reason (an agent can already delete every entity) does not hold alone, since emptying is undoable and removing destroys the undo log; what makes the verb acceptable is the 7-day soft-delete backstop, still invisible in the product, which is B109. Four mutants bite; a fifth correctly survives, because `snapshotBody` already refuses an unauthorized principal. |
| **B33** | **No authentication anywhere; CORS is `*` on every response including writes.** `rest.js:18` sets `Access-Control-Allow-Origin: *` with no `OPTIONS` handler, so any web page can read any diagram from a reachable instance; writes are protected only by the preflight failing on `X-Draw-Lock`. The lock is an arbiter, not a credential — `reclaim` takes no token by design. The ws endpoint has no origin check and is not subject to CORS. Default bind is `127.0.0.1`, but the documented container path sets `HOST=0.0.0.0`, which removes the only real control. Consistent with **N4** (no authn/authz) — recorded here because the *reachability* consequence of N4 is not stated anywhere. | `[V, server/rest.js:18; no OPTIONS handler — app.js:144 answers 405; server/app.js:160 no origin check; Dockerfile HOST=0.0.0.0]` | PART-CLOSED -- H9.28 took the cheap half the disposition named. **The row's opening sentence was stale and is corrected here: authentication exists** (IAP since H9.15) and reads are grant-gated (B67), so "any web page can read any diagram" has been false since. What remained true was the wildcard CORS header and the absent websocket origin check, and the second is the one that mattered: CORS does not gate a websocket, so a cross-site upgrade carries the victim's cookie and IAP admits it. **The trigger fired at H9.15 and nothing happened** -- a parked row whose revival condition passes unnoticed is the anti-amnesia mechanism failing, which is worth more attention than the defect was. Still OPEN: the `reclaim` verb takes no token by design, which the row correctly calls an arbiter rather than a credential. TRIGGER for the remainder: any requirement for a second human writer on one diagram. |

## Axiom review 2026-08-19 — rows B34–B36

Filed by an alignment review of the CS-close audit and `docs/BOARD.md` against `mission-kit/axioms`.
drawv2 satisfies `any-system`, `stateful`, `declarative`, `multi-agent`, `llm-in-the-loop` and partially
`autonomous`, so effectively the whole axiom set is in force. The review found the two H0/H1 commits
axiom-clean (**A8** Law of Fallback and **A14** capture-before-fix upheld to the letter) and the board
substantially sound — but caught **three register leaks**: findings and deferrals that existed only in
conversation or only in the mutable board. That is **A14 Insight Depreciation** and a breach of this
file's own "Adding a row" contract, which requires a row *whenever a design decision defers work*.

*(The geometry / connection-grammar deferral in the board's "Not in this arc" deliberately gets **no**
row: `docs/spec/HIERARCHY.md` §0 and §7 plus `design/walk/FINDINGS.md` are already its durable record.
A duplicate row would be ceremony, not zero-loss.)*

| # | Row | Evidence | Closes / Trigger |
|---|---|---|---|
| **B34** | **`commitSelection` broadcasts the whole document where every other write broadcasts a delta.** Setting a selection ships a full `snapshot` to every viewer (`rest.js:114`), while the commit path one line earlier ships `change {…, ops}` (`rest.js:100`). Selection is the *highest-frequency, lowest-information* write in the system — an agent sweeping focus across a diagram re-transmits the entire document per step. **A12 Projection, not dump**: LLM-facing and viewer-facing payloads must be engineered projections, not raw state dumps, and shaped at the emission source. D7 already ruled that the server broadcasts a Change, not a snapshot; this is the one write path that never got the memo. Found in the CS-close audit and **filed nowhere until this review** — the A14 fault the review itself was looking for. | `[V, server/rest.js:104-116 (the snapshot broadcast at :114) vs server/rest.js:99-101 (the delta broadcast); D7]` | **CLOSED H4** — selection is a **first-class event**, `selection {ids, actor}`, on BOTH transports. REST shipped an entire document snapshot for a focus change (A12 *Projection, not dump*; D7 had already ruled the server broadcasts a change, and this was the one write path that never got the memo) while the websocket broadcast **nothing at all**, so two viewers never shared a selection. Neither was right. `actor` rides it from the start because the point of the event is watching an agent work, and a future view or animation must attribute a transition to someone. |
| **B35** | **`app/src/input.js` is a God Object, and the deferral had no durable home.** 1,609 lines, ~40 methods, 11 gesture modes, 16 independent state fields with no state object, and 23 of the 29 commit sites in the app. Four functions carry it: `onKeyDown` **224** lines, `onUp` **169**, `onDown` **162**, `onMove` **101** — `onKeyDown` alone is a flat if-ladder with three *implicit* priority tiers discoverable only by reading top to bottom, which is the direct cause of **B18** (guards below the branches they must gate) and **B11**-class ordering leaks. This is **A3 God-Object Accretion** and a **Law of One** breach ("a module does exactly one thing"). Deferred deliberately — a refactor without a net is how **B14** happened — but the deferral lived only in `BOARD.md`'s mutable "Not in this arc" section, so clearing the board would have erased it. | `[V, counted: wc -l app/src/input.js = 1609; onKeyDown 1363-1586, onUp 811-979, onDown 178-339, onMove 660-760; 23 history.commit sites]` | **H6** — promoted from deferral to a scheduled arc (approved 2026-08-19), running after H3–H5. Approach, measurement and staging in `docs/BOARD.md` §H6. Governed by **A3**: six units, each stating its duty in one sentence, of which only **three are new files** — `pick.js`, `overlay.js`, `keymap.js`; drag-geometry folds into `snap.js` and the intent builders into `commands.js`, because A3 *Earned Exposure* says a concern earns a boundary by being one concern, not by being noticed. Decisive finding from the coupling analysis: **4 of the 11 gesture modes already carry a `start*/update*/commit*` triple** and the other 7 are inlined in `onUp` — the uniform shape is latent and the if-ladder is hiding it, so H6.4 finishes a design that is already half-present. Staged so only the last stage is a rewrite: 312 stateless lines move first at zero risk, then state gets owners, then the dispatchers invert. Still gated on **B23** — the net is written once at H2.1, asserts only at the commit boundary (sovereign to gesture production per D4), and must be **unchanged** at the end, which is the proof that behaviour was preserved. |
| **B36** | **Five known performance costs, all currently sub-threshold, none measured.** `nodeAt`/`endpointAt` linearly scan `all('node')`/`all('waypoint')` on **every pointermove** and again on every `onUp`, while the engine already maintains an O(1) cell-occupancy index used only for stamp gating (`input.js:157, :590`). `contentSig` runs `JSON.stringify` over the whole content array on every `set` for every content node (`renderer.js:26, :156`). `Model.nextName` rebuilds a Set of every name and linear-probes, once per cloned entity, making an N-entity clone O(N·total) (`model.mjs:148`). `onDblClick` builds three filtered+sorted arrays over all nodes (`input.js:1228-1239`). `dataview.render` wipes and rebuilds the entire overlay each frame. No axiom mandates a fix — recorded so the deferral is durable rather than board-local, and so the first document that makes one of them show has somewhere to land. | `[V, all five sites read; app/src/input.js:157, :590, :1228-1239; app/src/renderer.js:26, :156; model/model.mjs:148; app/src/dataview.js:50-69]` | **PARTIALLY CLOSED H5.4** — the cross-cutting half is done. `spanExtent(span, V)` now owns `(span.cols - 1) * pitch`, which had **five spellings**: `spanPx` (renderer) and `spanExt` (input) were the same function under two names, and the readout and the double-click hit-test each inlined it again — the last of those now reuses `inFootprint` rather than re-deriving the rectangle. `scan-twins` could not see any of it: they are one-liners below its `MIN_LINES` floor, which is a stated limitation of that detector, not an oversight. Two `span` computations deliberately survive because they answer different questions with the same field — `engine/relations.mjs` keys occupied CELLS, `kernel/adapt.mjs` builds cell RANGES. **CLOSED H6.6** — the remainder, all three. (1) **Two crosshair owners**: `Overlay` and `Palette` each constructed one on the SAME `#snaplayer`, so two could be drawn at once. Measured: never reachable, because `onDown` calls `palette.hideHand()` before starting a gesture — correct by remembering, not by construction, which is B43's shape. `main.js` now owns one instance and injects it into both; a second is impossible rather than merely absent. (2) **The zone-corner map**: `overlay.js` placed handles at the ACTUAL corners and `input.js` listed, per handle, the OPPOSITE corner to pin — the same four expressions transposed, with the mapping existing only in the reader's head. Now `zoneCorners(z)` plus an explicit `OPPOSITE_CORNER` in `snap.js`, so the relationship is the thing stated. (3) **Two drag thresholds**: NOT a duplicate, and the investigation is the result. `DRAG_THRESHOLD` (4) is CANVAS units measured after `toCanvas`, so it survives pan/zoom; the palette's bare `5` is SCREEN pixels on raw `clientX/Y`, because a tile is chrome outside the canvas transform. They are equal only at 1:1 zoom, so collapsing them would be a bug. Named `CLICK_SLOP` and the distinction written down: two constants was right, two ANONYMOUS constants was not. ||

| **B37** | **Server-Locked is too RESTRICTIVE as well as too permissive — the guard is positional, not semantic.** `SCOPE.md` decision 5 promises that while locked *"selection, the data view, and the readout still work, but no mutations."* Two of those three are broken: **Ctrl+A** (`app/src/input.js:1559`) and **Space/datum** (`:1457`) both sit BEHIND the blanket `if (this.readOnly) return;` at `:1444`, so a locked viewer cannot select-all and cannot set a datum. `Tab`/dataview sits in FRONT of the guard and correctly works — which shows the line's position, not any policy, is what decides. This is **B18 inverted**: B18 is three mutation paths ABOVE the guard, this is two inspection verbs BELOW it, and the single root cause is that read-only is enforced by *where a branch sits in a 224-line ladder* rather than by whether the verb mutates. Found by the H2.1 harness on its first run, from a test asserting SCOPE's own wording. | `[V, app/src/input.js:1444 guard vs :1559 Ctrl+A and :1457 Space; :1432 Tab is in front and works; docs/spec/SCOPE.md decision 5 amendment]` | **CLOSED H3.1, with B18** — the same edit, both directions. Ctrl+A and Space/datum moved above the boundary; `Tab` was already there. The durable fix is **H6**'s `keymap.js`: each entry declares its intent AND whether it mutates, and the read-only gate filters on that flag instead of on line order. Until the table exists, the two guards must be placed by hand and are one refactor away from drifting again. |

| **B38** | **The kernel scene has a dead `link` element kind, and `wire` exists only to accommodate it.** `geometry.mjs` exports `link(x1,y1,x2,y2)` and **nothing anywhere constructs it** — `resolve()` pushes `node`, `waypoint`, `zone`, `group`, `path` and never `link`. Yet four consumers handle the kind (`grc.mjs:16,30`, `renderer.mjs:76,137`), and `grc`'s `wire` abstraction (16 refs) exists for the sole purpose of unifying a LIVE kind (`path`) with a DEAD one (`link`) so the rule-checkers can iterate both. Retiring the dead element collapses the whole knot: GRC iterates paths directly, `wire` disappears, and the word `link` is freed to mean exactly one thing — the document entity — instead of naming two different things in two layers, which is the only genuine cross-layer collision in the system. Found while ratifying the connection taxonomy (`docs/spec/HIERARCHY.md` §0). | `[V, exhaustive grep: zero constructors of the scene `link` element; kernel/engine.mjs scene.push sites are node/waypoint/zone/group/path only; kernel/grc.mjs:14-17 wiresOf]` | **CLOSED H5.5** — the dead `link` element and its four consumers are gone, and `wire` collapsed with it: the union had exactly one live member, so `wiresOf` became `pathsOf`, a filter. The GRC's four turning-path rules now say *path*, which is what the ratified taxonomy calls an ordered list of coordinates (HIERARCHY §0). `link` means one thing again. Pinned by a **property** test rather than an absence — *every element kind the kernel handles is a kind `resolve()` can emit* — read from what `engine.mjs` imports, because reading it from `geometry.mjs` would have passed while the dead constructor still declared its kind. That test also surfaced `port` and `junction` as handled-but-unemitted; they are **declared, not dead** (`[LOCKED]` in ATOMICS, and anchors under the taxonomy) and are recorded as such rather than deleted — removing them would delete locked design work awaiting the connection arc. |

| **B39** | **The websocket waived D14's mandatory `expect` on undo and redo.** D14 is `[LOCKED]`: *"`expect` — optional on forward writes, MANDATORY on undo and redo"*, because those are the one pair of verbs whose target is implicit — the top of a ring another writer may have moved since you read it. REST enforced it. The ws gate was `const mine = cmd === 'undo' ? (top && top.actor === this.actor) : true;` — so **redo never required it at all**, and undo waived it whenever the caller authored the top record. "I wrote the top record" is precisely the belief D14 exists to distrust: the ring is shared (D2), and another writer can interleave between the read that formed the belief and the undo that acts on it. Found while unifying the two transports' reversal payloads (B17). | `[V, server/protocol.js undo/redo gate vs docs/spec/COMMIT.md D14; REST enforces at server/rest.js undo/redo branch]` | **CLOSED H4a** — tightened to match REST and the decision. Safe by construction: the browser already sends `expect` on every undo and redo (`app/src/changes.js:94-95`), so no client change was needed. Pinned by *"D14: the websocket refuses undo AND redo without expect, even for the author"* in `tests/server.test.js`, verified red first. |

| **B40** | **The two renderers share ~11 lines of text-layout maths, copied.** `app/src/renderer.js:contentDom` and `kernel/renderer.mjs:renderContentRegion` are **not** clones — an all-pairs similarity scan over 203 functions found this as the tree's ONLY pair above 25%, at 30%, and the shared part is interleaved with genuinely different emission code (live DOM vs string). But 11 normalised lines ARE byte-identical: the socket-union geometry (`x0 = oc*P - SE`, `w = (cols-1)*P + 2*SE`), the wrap algorithm (`cpl = max(1, floor((w-2*pad)/9))`, `lh = 18`), the alignment mapping and the anchor/fill resolution. Two renderers with two legitimate duties may both exist (B28); they must not both own the arithmetic. *(Corrects an earlier overstatement in this register that called them a “line-for-line port” — measurement says 30%, not 100%.)* | `[V, measured: normalised line-set intersection of app/src/renderer.js:31-62 and kernel/renderer.mjs:34-66 = 11 shared lines; all-pairs Jaccard over 203 functions yields exactly one pair ≥25%]` | **CLOSED H5.6** — `contentLayout(region, V, L)` in `kernel/renderer.mjs`, exported and used by both. It returns the box, the alignment and the **already-placed lines**, so neither caller re-implements the `rows <= 1` branch either; the two renderers keep their different duties and own only their emission. `hexColor` was a second, byte-identical copy and is now exported from the same place. The invariant pinned is the one the copy actually threatened: three tests assert that the kernel emits text at exactly the coordinates `contentLayout` computes, across five region shapes. `scan-twins`' ALLOW list is now **empty** — the tree's one twin was extracted rather than excused — and the scanner was re-verified to bite with nothing allowed. |

| **B41** | **`document/` was a three-way overloaded name.** The sovereign substrate was `document/`; the browser global is `document`; and "the document" is also what this system calls a persisted diagram (`docfile.mjs`, `validateDoc`, `doc.meta`). `app/src/main.js` carried all three within thirteen lines — `import { Model } from '../../document/index.mjs'` at `:10`, `document.getElementById('container')` at `:23`. A3 asks for one concept, one spelling; this was the inverse and worse — one spelling covering three concepts, so a grep for `document` in the client returned DOM calls, substrate imports and diagram-JSON prose interleaved. | `[V, measured before the rename: 32 code imports; `server/app.js` ×3 (the **`/document` HTTP mount** — the browser fetched the substrate over that path, so it was client-visible, not a file move); `Dockerfile` ×3; `tools/` scanners ×15; 7 docs files]` | **CLOSED H5.7** — renamed to `model/`, coordinated in ONE commit across the four surfaces that are normally independent: the filesystem (`git mv`, so history follows), the 32 relative imports, the `/document` → `/model` HTTP mount, and the `Dockerfile` COPY. Live-verified rather than assumed: `GET /model/index.mjs` → 200, the old path → 404, editor boots. Prose moved with the paths — a rename leaving the comments saying `document/` relocates the confusion instead of removing it. Both sub-decisions resolved as proposed: `model/model.mjs` kept (callers import `model/index.mjs`, so the stutter is almost never written), and `doc`/`docfile`/`validateDoc` deliberately **untouched** — a `doc` is the flat JSON that goes to disk and over the wire, a `Model` is the live object with methods, and `model.toJSON()` → `doc` is a distinction worth keeping named. |

| **B42** | **A tool armed BEFORE the Server-Locked handoff survived it.** H3.1 made the read-only gate semantic and gated `t`, the key that ARMS the text tool — but not the `textTool` branch in `onDown`, which sits **above** the read-only gate. The sequence *arm the tool, then get locked* walked straight past it: a text box was authored, applied locally, and dropped by `Sync` (`sync.js:62`). Permanent silent divergence — the exact failure B18 was about, surviving through a different door. `setReadOnly(true)` already cleared the stamp hand and the delete arming; the text tool was the one held tool nobody added, and that asymmetry is the tell. **Found by auditing the recognizer for `docs/spec/INPUT.md`, not by a test failing**: writing `onDown`'s fifteen ordered branches out as a table made a branch sitting above a gate it depends on visible at a glance. In a 167-line ladder, the ordering *is* the defect and the ordering is invisible — which is the argument for the recognizer being a table. | `[V, reproduced: arm `t`, `setReadOnly(true)`, press+release → 1 commit and 1 node applied locally; `app/src/input.js` textTool branch precedes the `readOnly` gate in `onDown`; `setReadOnly` cleared hand and arming but not `textTool`]` | **CLOSED H6 (design audit)** — `setReadOnly(true)` now clears every armed intent, not just the in-flight gesture. Two tests, both **verified red first**, the second asserting the tool still works after reclaim so the fix is a gate and not a wall. **Structural fix landed H6.13**: `textTool` moved onto `Palette` beside `hand`, and `releaseTools()` is the single call that drops every armed tool. Deliberately NOT merged into one value — a hand stamps on click and needs a type, the text tool drags a frame and is a mode, so collapsing them would force every `hand` consumer to special-case a type that cannot be stamped. What they share is a LIFECYCLE, and that is what B42 actually was: the list of things to clear lived at each call site, so one site forgot one tool. A third tool is now added inside `Palette` rather than at every site that has to remember it. |

| **B43** | **The gesture-end hook fires twice on a resize commit.** `onUp` wraps `dispatchUp` in `try/finally { onGestureEnd() }`, and `dispatchUp` ALSO ends with `this.onGestureEnd()`. Every branch of its mode ladder returns early **except** `resize`, so `resize` — and only `resize` — falls through to the trailing call and fires the hook twice. Harmless today purely by luck: the sole listener is `sync.releaseDeferred`, which drains-and-clears and is therefore idempotent (`sync.js:40`). The next listener need not be. This is the same class of defect as B42 — a trailing statement whose reachability depends on the early-return pattern of fourteen branches above it, invisible in a 168-line ladder and obvious the moment the ladder becomes a table. | `[V, reproduced: drag a zone's SE handle, count onGestureEnd calls → 2, expected 1; app/src/input.js:946 trailing call vs :777 finally]` | **CLOSED H6.4** — the trailing call is deleted; `onUp`'s `finally` is the single owner, so every mode fires it exactly once by construction rather than by each branch remembering to return. Test drives a real SE-handle resize and asserts the count. |

| **B44** | **Six commands are built inline in `input.js`, and each one bypasses a rule `commands.js` exists to enforce.** `commands.js:9` states the invariant plainly — *“No entry carries `before` any more; the inverse is derived server-side by the planner”* — yet `resize`, `replug`, `retype` and `close path` all pass a `before`. It is pure dead weight: `changes.js:25` maps a `set` entry to `{op, kind, id, patch: entry.after}`, so `before` is dropped before the wire and the server re-derives it from its own model (`txn.mjs:174`). Nothing reads it; a reader who sees it reasonably assumes it matters. Separately `commands.js:22` requires entities to be **cloned** at the command boundary so a history entry never aliases the live store — but `commitRoute` and `linkSelectedNodes` build their `put` entries with a shallow `{...link}`, and `via` is a COMPOSITE field (`model/shape.mjs:25`), so the entry's `via` array IS the model's. **Neither symptom is live**: `before` is discarded harmlessly, and no path mutates a `via` array in place (`model.set` uses `Object.assign`, which replaces the reference). Both are traps rather than bugs — the defect is that a documented invariant is stated in one file and violated in another, which is what building commands outside their builder costs. | `[V, app/src/commands.js:9 + :22 vs app/src/input.js resize/replug/retype/close-path carrying `before`, and commitRoute/linkSelectedNodes spreading `{...link}`; changes.js:25 drops `before`; model/shape.mjs:25 lists `via` as COMPOSITE]` | **CLOSED H6.2 Tier B** — all six move into `commands.js` as named builders. `before` goes with them; the `put` builders use `clone(kind, entity)`. Both invariants now hold by construction, because there is nowhere left to construct a command by hand. |

| **B45** | **`Input` reaches four DOM globals, and one of them duplicates a lookup `main.js` already did.** `input.js:367` resolves `document.getElementById('help')`; `main.js:49` resolves the SAME element independently, so two modules own one node and a change to the id has two places to be right. The other three are event surface: two `window.addEventListener` for keys and one `window.dispatchEvent` for the W5 `draw:action` handoff. Every other collaborator already arrives injected — `svg`, `model`, `history`, `renderer`, `readout` — so this is the composition root leaking into the one class H6 exists to decompose. **Measured context**: 10 of 18 client modules already reach zero DOM globals, including every unit H6 extracted (`pick`, `snap`, `overlay`, `recognize`, `keymap`) plus `renderer.js`, whose apparent DOM use is entirely through an injected `svg`. Input is the only logic module still reaching out. | `[V, app/src/input.js:367,411,412,492; the same `#help` resolved again at app/src/main.js:49; measured 2026-08-19: changes/commands/keymap/net/overlay/pick/recognize/renderer/selection/snap = 0 global DOM reaches]` | **CLOSED H6.5** — `help` and a `host` event surface are injected; `input.js` joins the DOM-free set as the eleventh module. GR17 seals it: only `main.js`, `painter.js`, `palette.js` and `labeledit.js` may reach a DOM global, the last two because building their own UI is their job. |

| **B46** | **~210 lines of `input.js` are command builders written outside `commands.js`.** 21 methods (302 lines) never touch `mode`, `ctx` or an event — they take ids and positions and produce commands. `wrapInZone` is 18 lines of pure geometry then `history.commit(commands.createEntity(...))`; `nudge` is pure computation then `history.amend(commands.moveEntities(...))`. This is B44's pattern in a form **GR16 cannot see**: the rule catches a `{label, entries}` literal, and these call a builder, so they pass. `commands.js` already does heavy domain computation (`deleteSelection`'s 55-line cascade), so the precedent is settled. They divide on one measurable property: **7 methods (~95 lines) are PURE** and could move today; **5 (~114 lines) ALLOCATE** — `cloneClosure`, `linkSelectedNodes`, `placeWaypoint`, `stampAt`, `commitRoute` must reserve ids, and `newId(kind, model.collection(kind))` reads the collection, so they eagerly `put` to stop the next id colliding and the commit re-puts idempotently. That single root cause is what keeps half of them out. **Explicitly NOT an `operations.js`**: a module described as “clone, and wrap, and nudge, and stamp” is A3's God-Object Accretion fault verbatim, and would relocate the defect rather than fix it. | `[V, measured 2026-08-20 by brace-matched extraction over comment-masked source: 70 methods, 804 lines = 59% of 1349; category D = 21 methods / 302 lines with no `this.mode`/`this.ctx`/`evt` reference; app/src/input.js wrapInZone, nudge, cloneClosure]` | **CLOSED H6.11** — both halves. The pure four landed at H6.9; the allocating three at H6.11 via `model/projection()`, the planner's own scratch-Model trick promoted to the substrate now that a second consumer exists (A3 Earned Exposure met by demonstration, A2 by giving both sides of the wire the same shape). `cloneSubgraph` and `linkNodes` allocate against a namespace that already contains the batch, so the eager `put` is gone rather than renamed — an allocator object would have fixed ids and names and left `linkNodes` holding its put for the `linkBetween` check. `commitRoute`'s put was simply redundant and is deleted. **`input.js` 1353→1218; `commands.js` 237→412.** Two `model.put` calls remain in `input.js` and both are declared live-preview writes (I-IN5), not allocation: the clone drag and the mid-route bend must be visible. Also fixed on the way: `cloneEntities` was the last put-builder still spreading entities shallowly, against B44's own rule. |

| **B47** | **`preventDefault` is handler boilerplate for a property of the BINDING.** 20 of 26 keymap handlers open with `evt.preventDefault()`; six do not (`onShiftDown`, `onEscape`, `onTextTool`, `onReshape`, `onHandDigit`, `onPipette`). Whether a key surrenders the browser default is structurally the same kind of fact as `mutates`, `duringHelp` and `duringGesture`, all of which already live in the table — so it belongs there as a flag the dispatcher applies once, not as a line repeated 20 times. **The blocking question is the default**, and it is a real question rather than a formality: it is not knowable from the code whether those six omissions are deliberate or accidental. `Escape` may be load-bearing; `q` and `1-7` look like drift. Until each is decided the six are indistinguishable, which is itself the defect. | `[V, app/src/keymap.js 26 run: targets cross-referenced against their handler bodies in app/src/input.js; 20 call preventDefault, 6 do not]` | **CLOSED H6.8** — the question is answered from history: git shows all six handlers were born in H6.4's extraction, and the pre-H6.4 ladder never called `preventDefault` for any of them either, with no comment anywhere explaining it. **Inherited, not decided.** `prevent` is now a table field defaulting to TRUE — bind a key and you own it — with seven reviewed opt-outs carrying two distinct reasons: `escape` because exiting fullscreen and cancelling an IME composition are the browser's, and `alt`/`control`/`dataview`/`waypoint`/`stamp`/`delete` because they claim the key only on the path that ACTS, which is a runtime condition a static table cannot see. Hoisting removed the call from 17 handlers; five keep it deliberately. The scan also corrected the row's own arithmetic: **17 unconditional, not 20** — the earlier count came from the same broken span extraction that inflated B48. |

| **B48** | **The keymap resolves the KEY but not the INTENT, and in one case the table is actively misleading.** `plain(e)` deliberately ignores `shiftKey`, so five handlers re-branch on it internally: `onDatum` (set vs CLEAR), `onArrowKey` (nudge vs resize), `onUndoKey` (undo vs **redo**), `onGroupKey` (group vs ungroup) — four different VERBS — and `onChainKey` (chain vs star), a parameter of one verb. The sharp case is history: the table carries a `redo` entry, but it matches only `Ctrl+Y`. **`Ctrl+Shift+Z` matches `undo`** — `is(e,'z')` ignores Shift — and is redirected to redo inside the handler. A reader of the table concludes `Ctrl+Shift+Z` undoes. It redoes, and two code paths reach the same verb. This is exactly the failure the table was built to remove: **B42 was found by writing a ladder out as a table and seeing what the ordering hid**, and a table that under-reports its own key surface cannot do that job again. The table CAN express it (`&& !e.shiftKey`), so this is a choice, not a limitation. | `[V, app/src/keymap.js:73-74 — `undo` matches `meta(e) && is(e,'z')` with no shift test, `redo` matches only `is(e,'y')`; app/src/input.js onUndoKey branches `evt.shiftKey ? redo : undo`; 5 keymap handlers read shiftKey, measured after correcting a span-extraction error that had inflated it to 12]` | **CLOSED H6.7** — split where Shift selects a different verb (4 new entries, 27→31); keep `chain` merged but the table passes `star` explicitly instead of the handler re-reading the event. |

| **B49** | **`scan-docrefs` answered differently on different machines.** It resolved every documented path with `fs.existsSync`, so a reference satisfied by a GITIGNORED file present on the developer's disk counted as resolved. Green locally for the whole of H5–H6; **red on the very first CI run** (H7), which is the only reason it was found — a fresh checkout has no `secrets/google-credentials.json`, no `secrets/google-token.json` and no seeded `diagrams/diagram-000001.json`. The three are legitimate references (README tells the user where to put their OAuth client; the store seeds `diagrams/` from `examples/` on first boot), so the citations are correct and the RESOLUTION was wrong. A guardrail whose verdict depends on whose machine it runs on is not a guardrail, and this one had been quietly weaker than its output claimed for two milestones. | `[V, CI run 32323021912 on the first push: `FAIL — 5 reference(s) point at nothing`, all five absent from a clean checkout and present locally; `git ls-files` confirms none is tracked]` | **CLOSED H7** — resolution is against `git ls-files` (plus derived directory prefixes) rather than the filesystem, which is also what a doc reference MEANS: a reader who clones this can find it. The three runtime paths are ALLOW rows with reasons. Verified machine-independent by citing a file that exists on disk but is untracked — BROKEN before `git add`, resolved after. |

| **B50** | **`README.md` is non-conformant to mission-kit's doc style, and the unenforced parts have rotted.** Measured with mission-kit's OWN enforcers (`tools/s6`, `s8`, `s10`, `s12`, `s13` at `origin/main` 4ee90e9): **S13** 52 non-ASCII characters in prose; **S10** 0 of 8 required rules between H2 sections; **S6** 60 mid-sentence wraps; **S12** 5 code blocks separated from their introducer; **S8** a 188-char prose run inside a code block; **S9** first actionable command at line 38 against a ~30 budget. Structurally: **S9** is violated by a 27-line `M1-M5` milestone changelog in the status position — the rule's own test is *would a routine, uneventful run make this line wrong?* and it claims *183 node tests* against an actual **411**; and by reference material (`Persistence`, `Slides`, `REST` at 75 lines) sitting ABOVE the primary Use path, which is buried at line 221. **S4** has no Remove journey at all, Install split across two sections, Test one line. **S5** pins `~178MB` and `node:22-alpine` in prose. **S3** restates a contract `docs/spec/` owns. The one section that is still correct is the REST block — the only one a test enforces (**GR10**), which is the finding: here, unenforced documentation rots and enforced documentation does not. | `[V, mission-kit enforcers run against README.md 2026-08-20; s13=52 FAIL, s10=1 FAIL (need 8), s12=5 FAIL, s8=1 FAIL; test count 411 vs claimed 183; `grep -c Remove README.md` = 0]` | **CLOSED H7.3** — rewritten to S9 order: pitch, lifecycle status, then Install / Use / Test / Remove, with everything else below. **281 -> 214 lines**, first actionable command at line **18** against a ~30 budget. The OAuth tutorial moved to `docs/slides-setup.md` (S4: link the deep-dive, do not embed) and the controls table shrank to the seven gestures needed to start, with the in-app `/` overlay named as authoritative — which also retires the twin, since two lists that disagreed is how `1-6` survived against a `1-7` keymap. Verified with **mission-kit's own enforcers** at `origin/main` rather than a hand-rolled checker: `s6`, `s8`, `s10`, `s12`, `s13` all clean on both files. The rewrite also caught one claim I nearly carried forward — `CLIENT_DIR=/none` for API-only has no handling in `server/app.js`, which just resolves the path. Gate green, GR10 intact. **The scanner half is NOT done**: the style rules are not in `npm run gate`, so this can still rot, and that is filed separately. |

| **B51** | **drawv2's docs are not style-gated, so B50 can recur.** The README was rewritten to mission-kit's S1-S14 and verified with that repo's own enforcers (`tools/s6`, `s8`, `s10`, `s12`, `s13`), but nothing in `npm run gate` checks it — the checkers live in another repository and were run by hand. That is exactly the condition B50 diagnosed: **the one README section that stayed correct for two milestones was the only one a test enforced (GR10)**. A style rule verified once by hand has the same durability as the milestone changelog that rotted. | `[V, npm run gate runs six scanners, none of which reads markdown style; the H7.3 verification was performed by hand against ~/taceng/mission-kit at 4ee90e9]` | OPEN — options: vendor the five enforcers as a seventh gate scanner, or depend on mission-kit as a checked-out tool. Vendoring duplicates a rule that has one owner (S3 argues against it); depending on a sibling repo makes the gate need something outside this tree. Needs a ruling. |

| **B52** | **The Docker image stopped building, and the gate never noticed.** H7's X14 discharge added `"prepare": "npm run gate:install"` so a fresh clone installs the pre-push hook during `npm install`. The Dockerfile copies only `package.json` + the lockfile before `npm ci` (`Dockerfile:14-15`) and never copies `tools/` at all, so `prepare` fires against a script that is not there: *`sh: can't open 'tools/install-hook.sh'`*, and `npm ci` exits 2. I had made `install-hook.sh` tolerant of a missing `.git` for exactly this case, but the guard is INSIDE the script and the script is absent, so it never runs — the tolerance was placed one layer too deep. **Broken and pushed at `0bff978`, undetected until the image was built by hand.** CI did not catch it because the workflow runs `npm ci` + `npm run gate` over a full checkout, where `tools/` exists; nothing in the gate builds the image. | `[V, docker-compose build fails at `Step 6/23 : RUN npm ci --omit=dev` with `can't open 'tools/install-hook.sh'`, exit 2; `grep -n COPY Dockerfile` shows no `tools/`; introduced by 0bff978]` | **CLOSED H7.5** — `prepare` is now `[ ! -f tools/install-hook.sh ] || sh tools/install-hook.sh`, so an absent script succeeds while a PRESENT one still propagates its real exit code (`\|\| true` would have swallowed genuine failures). Image builds and runs; verified end to end. **The gate gap is not closed** — nothing still builds the image, so the next Dockerfile regression is equally invisible. Filed as B53. |

| **B53** | **Nothing in the gate builds the Docker image, so packaging regressions ship silently.** B52 broke `npm ci` inside the image and survived a green gate, a green CI run and a push, because both run against a full source checkout where every path exists. The image has a genuinely different filesystem — `tools/`, `tests/`, `docs/` and `.git` are all absent — and that difference is exactly where B52 lived. The README documents the container as the primary deployment (*“the whole product ships as one image”*), so the artefact the project leads with is the one artefact no check exercises. | `[V, .github/workflows/gate.yml runs `npm ci` + `npm run gate` only; `npm run gate` = suite + six source scanners, none of which invokes docker; B52 reached origin/main undetected]` | OPEN — add a CI job that builds the image and probes `/health`. Note the local hook cannot reasonably do this (a multi-minute build on every push), so this one is CI-only by nature, which is itself a change to what “the gate” means. |

| **B54** | **A half-open websocket is never evicted: the session FSM has no liveness probe.** `Session` is added to the `Hub` on connect and removed on `ws.on('close')` (`protocol.js:147,149`), which is correct for every disconnect that produces a TCP FIN — verified for graceful `close()`, `terminate()` and a raw `_socket.destroy()`, all three cleaning to zero. But there is **no ws-level ping/pong anywhere in the server**, so a peer that vanishes WITHOUT a FIN (lid closed, network partition, NAT idle-timeout) leaves the session in the hub indefinitely with `readyState === 1`. Meanwhile `net.js:9` retries every 3s and establishes a NEW session, so the stale one accumulates beside it holding a socket FD. Not a correctness fault — broadcast is per-session isolated and `Session.send` no-ops on a non-OPEN socket — but it is an **unhandled state rather than a handled one**, and it matters most in the long-running container the README leads with. Measured alongside: the `Hub` try/catch (GR12) is effectively **unreachable through a real socket**, because `send()` guards `readyState` and `ws.send()` into a vanished peer does not throw; its test proves the catch works against a synthetic thrower, which is defence in depth rather than a demonstrated path. | `[V, grep for ping/pong/isAlive/heartbeat across server/ returns nothing bar unrelated lock heartbeats; probe against a real WebSocketServer wired as app.js:188 shows inHub=1 readyState=1 immediately after the peer is terminated, settling to inHub=0 readyState=3 only once close fires]` | **CLOSED H7.7** — a single server-side ping sweep marks each client dead-until-pong and terminates one that misses a round, which produces the `close` the FSM already handles rather than adding a second eviction path. |

| **B55** | **The store's persistence seam is one verb wide, and the cloud needs four.** `D19` injected `writeDoc(file, text)`, which covers the flush and nothing else. `Store` also reaches `fs` directly for `readdirSync` and `readFileSync` at boot (`store.js:75,79`), three `rmSync` calls on delete (`:250-252`), and `mkdirSync` (`:70`). So a GCS implementation cannot be supplied by injection today: three of the four operations bypass the seam. Note the delete does THREE removals -- the entry file, the canonical name, and a `.json.tmp` -- and that third one exists only because the filesystem write is a write-then-rename. Against an object store there is no temp object and the call is vestigial. Also note what must NOT move: `examples/` is read-only content baked into the image (`:140-144`) and stays on the filesystem, while `diagrams/` is mutable state and is the only thing the adapter owns. | `[V, exhaustive grep of fs.* in server/store.js: writeFileSync/renameSync :63-64, mkdirSync :70, readdirSync :75, readFileSync :79, existsSync/readdirSync/readFileSync :140-144 (examples), rmSync x3 :250-252]` | **CLOSED H8.1** -- `server/files.mjs` owns the surface: `{list, read, write, remove}`, filesystem default, injected whole. Verbs take NAMES rather than paths, which is the load-bearing detail -- passing `path.join(dir, file)` around would push filesystem shape into a backend that has no directories. The vestigial `.json.tmp` removal is gone from the caller: the temp artefact belongs to whichever write strategy the backend uses. Pure refactor, 412 tests unchanged and green. **Proven by a store running on a `Map`** with no directory ever created -- filesystem-only tests would pass whether or not the seam were real, and reintroducing a single `fs.readdirSync` behind it makes that test fail. `examples/` deliberately still reads from disk: it is read-only content baked into the image, not mutable state. |

| **B56** | **The Slides refresh token is written to a filesystem Cloud Run does not keep.** `server/slides/auth.js:22` persists it at `<secretsDir>/google-token.json`, and the Cloud Run filesystem is ephemeral, so every deploy and every instance replacement discards it and the next push demands re-authorization. `min=1` makes this rare rather than constant, which is worse: it will present as an intermittent mystery rather than a reproducible fault. The credentials file is not affected -- it is read-only and can be mounted from Secret Manager. It is the WRITTEN token that has no home. | `[V, server/slides/auth.js:22 tokenFile resolves under secretsDir; :50 reads it; the OAuth callback writes it; Cloud Run's container filesystem is in-memory and per-instance]` | OPEN -- simplest resolution is to keep it in the bucket the adapter already talks to, which needs no Secret Manager write permission and no second integration. |
| **B57** | **A Testing-mode consent screen expires the Slides refresh token every 7 days, and durable storage will not save it.** Google issues a 7-day refresh token to any External consent screen whose publishing status is `Testing`, exempting only apps whose scopes are a subset of name/email/profile. `server/slides/auth.js:14` requests `auth/presentations`, which is outside that exemption, so the token dies weekly regardless of where it is kept. This is NOT B56: B56 is a token with no durable home, and moving it into the bucket fixes storage while leaving the expiry untouched. The two present as the same symptom -- a push that suddenly demands re-authorization -- which is exactly why they must not be conflated, since fixing B56 alone would look like a fix for about a week. IAP sign-in is unaffected and needs no ruling: it requests only `openid`/`email`/`profile`, which IS the exempted subset, so Testing mode costs it nothing beyond the test-user list. | `[V, developers.google.com/identity/protocols/oauth2#expiration: "a publishing status of 'Testing' is issued a refresh token expiring in 7 days, unless the only OAuth scopes requested are a subset of name, email address, and user profile"; server/slides/auth.js:14 SCOPE = auth/presentations; gcloud iap oauth-brands list shows orgInternalOnly absent, so the brand is External]` | OPEN -- needs a ruling, and the options differ in kind rather than degree. Publishing the screen removes the expiry but invites verification for a sensitive scope. Domain-wide delegation via a service account drops the user-consent flow altogether and is available because the org is a Workspace domain. Accepting a weekly re-auth is defensible for a single-operator tool, but only if the failure is made legible rather than intermittent. |

## Adding a row

A row is required whenever a deletion removes a capability that nothing restores
(`docs/spec/COMMIT.md` §7.4 column (d)), whenever a design decision defers work, or whenever a defect is
found and not fixed in the same commit. Cite evidence as `[V, file:line]` or `[V, grep]`. If the row is
deferred rather than closed, the trigger must name an observable condition — not "later".
| **B58** | **The cutover creates the certificate before the DNS that validates it, and the retry backoff punishes the gap.** `DEPLOY.md:117` creates `ssl-draw-apnex-io` at step 3; `DEPLOY.md:119` moves DNS off the bucket at step 5. A classic managed certificate only validates once the domain resolves to the load balancer, so every attempt between those two steps fails against a name still pointing at `c.storage.googleapis.com`. That is not a harmless wait: domain validation backs off exponentially, so the certificate can stay dark long past the nominal 60 minutes AFTER the flip finally lands -- and by then v1 is already gone, because the CNAME it was served by is what step 5 removes. Google's own guidance inverts this order, creating the certificate at the cutover rather than before it. There is also no way to soften it by pointing the name at both: MPIC requires the records resolve ONLY to the load balancer address, so a bucket record left in place to cover the gap is itself a validation failure. The window is therefore real, unavoidable on this path, and currently unrecorded -- `DEPLOY.md` says nothing about provisioning, and line 122 offers CNAME rollback as though the transition were instant. | `[V, DEPLOY.md:117 step 3 cert, :119 step 5 DNS, no occurrence of provision/window/outage anywhere in the file; cloud.google.com/load-balancing/docs/ssl-certificates/google-managed-certs -- "To provision SSL certificates, ensure that A and AAAA records point to the load balancer's IP address at a public DNS", "Provisioning a Google-managed certificate might take up to 60 minutes", "point only to the IP address ... The existence of any other addresses in the record can cause validation to fail", and the recommendation to "create or recreate the certificates shortly before or during the maintenance window ... due to the timer backoff"; dig draw.apnex.io = CNAME c.storage.googleapis.com; certificatemanager.googleapis.com not enabled on labops]` | **CLOSED** -- ruled for Certificate Manager, and provisioned. `auth-apnex-io` authorizes `apnex.io` over an `_acme-challenge` CNAME in the `apnex-io` Cloud DNS zone, and `cert-apnex-io-wildcard` covers `apnex.io, *.apnex.io`. The defect is not patched but deleted: the certificate was created and validated while `draw.apnex.io` still resolved nowhere near the load balancer, which is exactly what a classic certificate cannot do and what made the original step order unworkable. Wildcards decided it as much as timing did. Classic managed certificates cannot issue one at all, so three per-hostname certificates collapse into a single certificate that already covers `draw.apnex.io` and every subdomain after it -- the fourth classic certificate this row was filed about is now never created. The apex is listed as its own SAN because a wildcard matches one label and not the bare domain. Two corrections to my own reading along the way. I first framed Certificate Manager as a second system running beside the classic four, which undersold a tool Google names the primary one. And I briefly mis-read a concatenated `--format=value` as showing `ssl-ois-apnex-io` detached from the proxy, and nearly filed that as a live misconfiguration; all three certificates are attached, and `ssl-labops-sh` sits on a different load balancer for a different domain and is out of scope. What remains is not this row: attaching the map is an atomic swap that makes the proxy ignore every classic certificate, so it belongs to the cutover with full coverage and a primary entry, and is tracked at H8.5. |
| **B59** | **The seam H8.1 widened is synchronous, and the backend it was widened for is not.** `server/store.js` contains no `async` or `await` anywhere, and all four calls -- `list` at `:73`, `read` at `:77`, `remove` at `:250-251`, `write` at `:356` -- are synchronous. `fsFiles` can honour that because `fs.readFileSync` exists; GCS over `fetch` cannot, because there is no synchronous HTTP. So `read(name) -> string` is unsatisfiable by the very backend the seam was built to accept, and H8.2 cannot proceed as scoped. Fire-and-forget writes are not an escape: `rest.js:172` flushes before acking on purpose, because *"a REST/agentic caller is one-shot -- it has no reconnect backstop, so an acked write must be on disk"*, and `tests/locks.test.js:199` asserts it. A promise the handler does not await is a promise the ack cannot make. The uncomfortable part is that B55 shipped a test I called proof. It ran the store on a `Map` and showed the seam was not filesystem-bound -- true, and worth having. But a `Map` is synchronous, so it demonstrated the seam accepts a class the real backend is not a member of. The property I verified was adjacent to the property that mattered, which makes this the eighth instance of a green result measuring the wrong thing. | `[V, exhaustive grep: zero async/await in server/store.js; this.files.list :73, read :77, remove :250-251, write :356 all called synchronously; files.mjs:37-55 all four verbs return values not promises; rest.js:169-172 comment and synchronous store.flush(id); tests/locks.test.js:199 "an acked write is already on disk (flush-before-ack -- no debounce window)"; callers needing to change: app.js:124 init, rest.js:172/188/368 flush, protocol.js:221 create, protocol.js:322 remove; the setTimeout flush at store.js:344 is the one path already tolerant of async]` | **CLOSED** -- the seam and `Store.init/flush/remove/flushAll` are async, awaited at every call site. One correction to this row as filed: `create` did NOT need it, because it only installs and marks dirty and never touches `this.files`; the async set is four methods, not five. Three hazards surfaced that the mechanical change would have introduced silently. `server.js` called `flushAll()` then `process.exit(0)`, which with an async flush discards every debounced write at shutdown -- and shutdown on Cloud Run is how a revision normally ends, so the target platform would have been the one place it bit. `protocol.js` wrapped `dispatch` in a try/catch whose comment says a crash vector must never take the server down; unawaited, that catch stops catching and a rejection escapes as an unhandled rejection. And the two `setTimeout` flush callbacks cannot await by construction, so both now catch and name the diagram rather than surfacing anonymously in the last-resort net. Proven by a backend that defers every verb across a real macrotask -- the property B55's synchronous `Map` could not reach. Dropping the `await` in `flush` fails the durability assertion, and dropping it in `init`'s read fails the round trip. 414 tests green. The alternatives are worse than they look: a synchronous cache over async GCS reintroduces exactly the durability gap `rest.js:172` exists to close, and sync-over-async via `Atomics.wait` or a subprocess buys a smaller diff with a permanently stranger runtime. Note this is a REVISION of H8.2's size, not a new task beside it. |
| **B60** | **The websocket URL hardcodes `ws://`, so the editor cannot load its document over HTTPS.** `app/src/main.js:82` builds `ws://${location.host}/ws` with a literal scheme. Served from `https://draw.apnex.io` the browser blocks that connection as mixed content, so `hello` never completes, no document ever arrives, and the canvas renders empty against a server that is holding eleven diagrams perfectly well. It also explains the *"Not secure"* indicator, which reads as a certificate fault and is not one: the wildcard is served correctly and verifies, and the browser is objecting to the insecure subresource the page itself asked for. Two symptoms, one cause, and both point away from it. This could not have surfaced before today -- every previous run was `http://localhost`, where a literal `ws://` is correct, so the defect has been latent since the client was written and was always going to appear on the first HTTPS deployment rather than in any test. | `[V, app/src/main.js:82 `new Net(\`ws://${location.host}/ws\`)`; page served over TLS at draw.apnex.io, cert CN=apnex.io SAN apnex.io,*.apnex.io issued by Google Trust Services WR3, verifies clean; /health on the same deployment reports 11 diagrams and 0 flushFailures, so the emptiness is transport, not storage]` | **CLOSED H8.6** -- `wsUrl({protocol, host})` in `app/src/net.js` derives the scheme, and `main.js:82` calls it. The derivation is a pure exported function rather than an inline ternary specifically so it can be tested without a page: the defect survived this long because nothing in the suite could reach the line, and a fix that stayed unreachable would only be the same gap with better wording. Three cases asserted -- https to wss, http to ws, and a non-default port -- and restoring the literal `ws:` fails the first. |
| **B61** | **The deployment closes the agent surface the README sells.** `README.md:3` calls draw *"drivable by a human or an agent"* and `:9` promises *"a server-side agent can drive the same document over REST while a human watches it change"*. Behind IAP that is no longer true: `GET https://draw.apnex.io/api/v1/diagrams` answers `302` to `accounts.google.com`, and the CLI sends no credential of any kind -- `cli/draw.sh:7` targets `${DRAW_HOST:-http://localhost:8080}` and every call is a bare `curl` with no `Authorization` header anywhere in the file. So the CLI works against localhost and is locked out of the only deployment that exists. This is not IAP misconfigured; IAP is doing exactly its job. It is that the product has two front doors and the deployment only opened one. The capability is now present rather than absent, which is what makes this worth fixing rather than merely noting: the custom OAuth client adopted at H8.6 supports programmatic access, where the Google-managed client it replaced did not. An agent authenticates by presenting an OIDC ID token whose audience is that client id, as `Authorization: Bearer`, which is a header the CLI has no notion of. Worth separating from B57, which is also an auth problem and an unrelated one: B57 is the Slides token expiring weekly, on a different OAuth client, for a different scope. | `[V, README.md:3 and :9 state the agent claim; live GET /api/v1/diagrams via the load balancer returns 302; exhaustive grep of cli/ finds no authorization/bearer/id_token handling; cli/draw.sh:7 APIHOST defaults to http://localhost:8080; cli/draw.sh:36,285,301 issue bare curl calls; cloud.google.com/iap/docs/custom-oauth-configuration -- Google-managed clients "cannot programmatically access IAP-protected applications", custom clients support "Browser flow and Programmatic access"]` | CLOSED -- H9.6. `/connect/v1` rewrites to `/api/v1` at the router, so an agent and the editor drive one implementation and neither can drift. A connection code presented as a bearer token resolves to the agent it was minted for, and the grant decides from there. The README's claim that draw is drivable by an agent is true over HTTP for the first time. Remaining: the load balancer must route /connect to a backend without IAP, which is an estate change and is raised separately rather than made unilaterally. |
| **B62** | **`scan-dead` counts a symbol's own file against it, so a comment or string literal satisfies the check.** `tools/scan-dead.mjs:81` computes `countIn(prodFiles, sym, file)` and passes on `prod > 0`, but the origin file is only discounted by one occurrence rather than excluded. So an export mentioned a second time anywhere in its own file -- a doc comment, an error message, its own default parameter -- reads as having a production consumer. Found by accident and worth recording as such: `server/identity.mjs` exported `iapIdentity` with the error string `'iapIdentity requires the backend service audience'`, and that string alone was enough to pass the gate for a module nothing imports. Removing four words from a message turned a green scan into `TEST-ONLY ... FAIL`. The scanner is not hollow -- a genuinely unmentioned export is caught, verified by adding one -- so this is a narrow gap rather than a broken check. It is still the shape that matters: the check reports a property nobody has, and the more carefully an author documents a symbol the more likely it is to exempt itself. | `[V, tools/scan-dead.mjs:81 `const prod = countIn(prodFiles, sym, file)` then `if (prod > 0) continue`; with the error string present scan-dead reported `223 export(s); 0 without a production consumer`; with the same string reworded to omit the symbol name it reported `TEST-ONLY server/identity.mjs:iapIdentity (tests: 2)` and FAILED; a control export `definitelyNobodyCallsThis` with no second mention was flagged immediately]` | CLOSED -- H9.11, and the defect was larger than this row described. It is not only that a comment or string can exempt a symbol: `hits - 1` discounts ONE occurrence in the origin file and counts the rest, so **any internal use** satisfied the check, and internal use is precisely what does not earn an export. The header of `scan-dead.mjs` has always said *a real consumer outside its origin*; the arithmetic said something weaker. Excluding the origin outright surfaced **18** exports that had been passing on their own internal references, against the one this row was filed for. Both remedies landed: the origin file is skipped, and `strip` now removes string literals as well as comments, so prose about a symbol is never a dependency on it. Of the 18, eight were used only inside their own file and lost the `export` keyword rather than the symbol -- `snap1`, `LOG_BYTES`, `LOG_HARD_BYTES`, `ackBody`, `syncBody`, `DEFAULT_SCALE`, `SCHEMA`, `MAX_COLLECTION` -- and ten are deliberate test seams that now carry the reason they exist. A third defect fell out: two ALLOW entries had outlived their cause, one of them naming its own expiry condition (*until `list()` filters by grant in the same milestone*, which shipped in H9.2b) and never being revoked. A stale exemption is not inert -- it would answer `allowed` where the scanner should answer DEAD if the consumer later disappeared -- so the scanner now reports them, as a note rather than a failure, because an exemption outliving its cause is bookkeeping and must not block the commit that legitimately ended it. |
| **B63** | **`ACCESS.md:204` overstates the lock hole: revocation does take effect, the residual damage is denial of service.** The line reads *"Revoking a principal's grant while it holds a lock leaves the lock working until its TTL expires"*, which is true of the lock and false of the consequence it implies. H9.3a gates `commit` on `access(id, principal)` at every call, so a revoked principal's write is refused the instant the grant is dropped whether or not it holds a lock -- the lock buys it nothing. What survives revocation is the lock as an obstruction: the revoked principal still occupies the single write slot, so the owner's own `acquire` is refused until the TTL runs out. Two further gaps sit next to it and neither is stated in the doc at all. `locks.js:41` `acquire(id)` takes no principal, so any caller that reaches the route takes the lock -- including a `read`-only grantee, who can then block the owner indefinitely by re-acquiring, and who is not writing anything so the H9.3a guard never fires. And `rest.js:309` calls `locks.acquire(id)` with no access check of any kind, one of the few mutating routes H9.3b did not reach, because acquiring a lock is not a store mutation and so was not in the seven-method sweep. The correction matters more than the wording: a hole described as a confidentiality failure gets a different fix, and a different priority, than the availability failure it actually is. | `[V, docs/spec/ACCESS.md:204 states the TTL claim; probe with authz on -- grantee acquires lock, owner revokes, grantee's commit returns `forbidden: no write access to this diagram` while `locks.verify(id, token)` still returns true and the owner's `acquire` is refused; server/locks.js:41 `acquire(id)` signature takes no principal; server/rest.js:309 `const lock = locks.acquire(id)` with no preceding access call; server/store.js `#mayWrite` covers commit/undo/redo/patchMeta/setSelection/bindSlides/remove and not lock acquisition]` | CLOSED -- H9.4, and with less code than the entry proposed. Gating `acquire` on `store.canWrite` was necessary and sufficient; the principal recording and eviction method were written, found to have no caller, and removed. Reclaim is already an unconditional force-release for the owner, so the remedy against a revoked squatter existed the whole time -- it only needed its own gate (B64). That is what makes the H9.4 premise on the board wrong as stated: locks did not need to become principal-scoped, `locks.js` is unchanged, and its `:8` sovereignty claim needed no amendment. Authorization belongs to the caller, which is where it now is. |
| **B64** | **`reclaim` is ungated, so any connected session can force-release any diagram's lock.** `protocol.js:315` handles the `reclaim` command with the comment *"No token: the browser user owns the tool"*, checks only that the diagram exists, and calls `locks.reclaim(id)`. That was true when every browser user was the owner. Under H9.1 grants it means a `read`-only grantee -- or, once open sign-in lands, any authenticated Google account that learns a diagram id -- can break a legitimate agent's lock at will, repeatedly, and the H9.3a guard never fires because reclaiming mutates no model state. It is the mirror of B63's acquire gap and was found while fixing it: the two routes that decide who may write are the two that write nothing. Also note the reverse reading, which is what makes gating it correct rather than merely safe -- reclaim being unconditional *for the owner* is precisely the remedy against a lock held by a principal since revoked, so gating it on write access closes the hole while keeping the eviction path B63 called for, with no code and no change to `locks.js`. | `[V, server/protocol.js:315-323 reclaim case checks only store.get(id) before locks.reclaim(id); with authz on, a Session constructed with a read-grant principal sent reclaim and the agent's lock was dropped -- locks.verify returned false; server/store.js #mayWrite covers no lock operation; mutating the added gate to `if (false)` fails tests/access.test.js `a reader cannot reclaim`]` | CLOSED -- H9.4. `reclaim` now requires write access via the new public `store.canWrite(id, principal)`, which is the same predicate `#mayWrite` phrases as an error string, so the rule has one home. |
| **B65** | **A diagram's creator is not its owner, so with `authz` on nobody can write a diagram they just made.** `store.js` declares `create(name, doc = null)` -- two parameters, no principal -- and sets no `meta.owner`, while `protocol.js:232` calls `this.store.create(name, body.doc || null)` and drops the session principal on the floor. The new diagram is therefore unowned, `canWrite` returns false for every principal including the one that asked for it, and the first commit is refused 403. H9.2b's `adopt(principal)` does not cover this: it claims unowned diagrams at boot from the `OWNER` env var, which is a migration for diagrams that predate the ACL, not a rule for ones minted at runtime. Found while checking whether the B2 offline-recovery path at `sync.js:192` should be suppressed for a reader -- it should not, because it creates a *new* diagram rather than writing the read-only one, and suppressing it would discard the user's offline work. That question is what exposed the real defect underneath: the recovery path creates a diagram the user is then locked out of, so B2's data-loss fix is undone by the ACL rather than by the lock it was written to respect. | `[V, server/store.js create(name, doc = null) signature takes no principal and no assignment to meta.owner appears in its body; server/protocol.js:232 `this.store.create(name, body.doc || null)`; no create call exists in server/rest.js; probe with authz on -- create a diagram as `user:guest@example.com`, resulting meta.owner is null and store.canWrite(id, GUEST) returns false; server/store.js:454 adopt(principal) is driven by the OWNER env var at boot]` | CLOSED -- H9.3c. `create` takes the principal and records it as `meta.owner`, which is the one place ownership can be established without trusting the wire: the value comes from the authentication boundary, never from `doc.meta`, so the `cleanMeta` trusted-flag defence from H9.1 stays intact. |
| **B66** | **`ACCESS.md:272` names the email header as the allowlist input, contradicting the correction three sections above it.** The line reads *"a domain allowlist belongs in the application, checked against `X-Goog-Authenticated-User-Email` before any grant lookup"*. That header is explicitly not a security input: `identity.mjs:10` quotes Google saying it is *"available for compatibility"* and that you *"shouldn't rely on"* it, and `iapIdentity` accordingly derives the principal from the signed `email` claim at `:99` and uses the header only as a cross-check at `:109-112`. The document was corrected in one place and not the other, so the section an implementer would read while building the allowlist still points at the spoofable value. The consequence is specific rather than theoretical: the header is client-settable on any request that reaches the app without passing IAP, and the allowlist is the one control whose whole purpose is to decide who becomes a principal at all -- reading it from an unsigned header would let an attacker choose their own domain. | `[V, docs/spec/ACCESS.md:272 names X-Goog-Authenticated-User-Email as the check input; docs/spec/ACCESS.md section "Identity comes from the assertion" states the opposite; server/identity.mjs:99 `const email = typeof claims.email === 'string' ? claims.email : ''` reads the signed claim; server/identity.mjs:109-112 reads the header only to report a mismatch and never returns it]` | CLOSED -- H9.8. The line now names the verified principal, and the allowlist is applied inside the authentication boundary rather than beside it, so there is no code path in which an unsigned header could reach it. |
| **B67** | **Reads were never gated: `hello` and `open` hand over the whole document to any session, authorized or not.** H9 gated writes (`#mayWrite` over seven store methods), list visibility (`list(principal)`), and lock acquisition, and left the document itself open. `protocol.js` `case 'hello'` resolves `(body.diagram && this.store.get(body.diagram)) || this.store.first()` and calls `this.snapshot(model)`; `case 'open'` does the same for an arbitrary id. Neither consults `access()`. `snapshotBody` then returns `doc: model.toJSON()` unconditionally -- it filters `diagrams` by principal in the very next line, which is what made the gap invisible: the list looks authorized, so the payload appears to be too. With no id at all, `store.first()` hands an arbitrary diagram to whoever connects. This is the defect that produced the observed symptom in production: with the ws principal unresolved, the owner saw `control-bar` render while the diagram dropdown stayed empty -- a document delivered past a filter that had already excluded it. The claim in the H9.3b commit that authorization is "identity in, grant checked, 403 out" was true only of writes, and I asserted it of the system. | `[V, server/protocol.js case 'hello' calls store.get / store.first then this.snapshot with no access check; case 'open' likewise; server/protocol.js:38-47 snapshotBody returns doc: model.toJSON() with no principal argument consulted, while filtering diagrams via store.list(principal) three lines later; observed live on revision draw-00004-6ts -- control-bar rendered in the editor while the diagram list was empty]` | CLOSED -- H9.12, and it was five omissions rather than two: ws `open`, ws `hello` via `store.first()`, `GET /diagrams/:id` and everything routed below it, and `/d/<id>.svg`. A `canRead` predicate mirrors `canWrite`, `first(principal)` no longer yields whichever entry the Map held, and `snapshotBody` throws outright so a future caller cannot rebuild the hole quietly. Each of the five is proven load-bearing by mutation. The remaining shape of the fix, as an `access()` check in `hello`/`open` before `snapshot`, plus a refusal in `snapshotBody` so a future caller cannot reintroduce it. `store.first()` needs the principal too, or it must return nothing when authz is on. |
| **B68** | **`iapIdentity` returns a bare `null` for at least eight distinct failures, so a broken deployment is undiagnosable.** Malformed header, wrong segment count, unparseable JSON, unpinned algorithm, unknown `kid`, bad signature, wrong issuer, wrong audience, and expiry all return the same `null` at `server/identity.mjs:64-97`, and both call sites then swallow anything thrown -- `server/app.js:174` and `:230` each append `.catch(() => null)`. A thrown error and a rejected token are therefore indistinguishable, and neither is logged. Live on `draw-00004-6ts` the effect is total: every commit logs `forbidden: no write access to this diagram` with no record of which principal was refused or why it was not the owner, and there is no way to tell an audience mismatch from a JWKS fetch failure without redeploying. The one thing the module does log is the email header cross-check, which fires only when verification has already succeeded -- diagnostics exactly where they are not needed. | `[V, server/identity.mjs:64,67,73,77,80,89,92,93,96,97 all return null with no reason recorded; server/app.js:174 and :230 both `.catch(() => null)`; live stderr on draw-00004-6ts shows 159 identical `[ session ] commit rejected: forbidden` lines and no identity diagnostic; the resolved principal appears in no log line anywhere]` | CLOSED -- H9.13. A named reason on failure, logged once per distinct reason rather than per request, so a misconfiguration announces itself instead of presenting as a uniform denial. The reason must never include the token. |
| **B69** | **WITHDRAWN -- stdout reaches Cloud Logging normally; the query was wrong, not the logging.** Filed on the observation that `gcloud logging read resource.labels.service_name="draw"` returned only `requests` and `stderr` streams and no `stdout`, and concluding that every `console.log` in the service was being discarded. That conclusion was false. Filtering instead on `resource.labels.revision_name` returned five `run.googleapis.com/stdout` entries immediately, including the boot lines the entry claimed were lost -- `[ draw ] persistence`, `[ draw ] authorization: on, adopting unowned diagrams for user:aobersnel@apnex.com.au`, and the H9.8 sign-in policy line. The `service_name` filter had silently matched nothing on that stream. Recorded rather than deleted because the failure is worth keeping: a diagnostic tool returning an empty result was read as evidence about the system instead of evidence about the query, and it was used to justify a code comment in `identity.mjs` asserting that stdout is unusable on Cloud Run. That comment would have misled every future reader. | `[V, gcloud logging read 'resource.labels.revision_name="draw-00005-4jz"' returns logName run.googleapis.com%2Fstdout with 5 app boot lines; the same window filtered on resource.labels.service_name="draw" returns zero stdout entries; no exclusion exists on _Required or _Default]` | CLOSED -- withdrawn, not fixed. The `identity.mjs` comment it produced has been corrected to state the real reason `console.warn` is used there, which is severity rather than availability. |
| **B70** | **`server.js` turns authorization on from `IAP_AUDIENCE` but never passes the audience, so the app verifies nothing and every request is anonymous.** `server/server.js:65` reads `createApp({ ..., authz: Boolean(audience), owner, domains })`. `audience` appears in that call only inside `Boolean(audience)`, so `createApp`'s own `audience = ''` default stands, and `server/app.js:136` resolves `principalOf || (audience ? iapIdentity({ audience }) : async () => null)` to the null-returning stub. The result is the worst available combination: the grant filter is ON, and no request can ever carry an identity, so `list()` returns nothing to anybody and `canWrite` is false for everybody -- including the owner the same file just adopted 12 diagrams for. Observed live on `draw-00004-6ts` and `draw-00005-4jz` as a read-only editor with an empty diagram list, and reproduced by running the deployed image locally: `GET /api/v1/diagrams` with no assertion header returns `200 []` and the B68 diagnostic never fires, because the code that would emit it is never constructed. The boot line `[ draw ] authorization: on, adopting unowned diagrams for user:aobersnel@apnex.com.au` prints truthfully from `server.js`'s own local variable while the app receives nothing, so the diagnostic and the defect sit on opposite sides of the same missing word. Every test calls `createApp` directly with `principalOf` injected -- the seam that makes identity testable is precisely what left this wiring unexercised, and 481 passing tests could not see it. | `[V, server/server.js:65 the createApp call contains audience only as Boolean(audience); server/app.js:110 signature defaults audience = ''; server/app.js:136 falls through to `async () => null`; deployed image 4513f50 run locally with IAP_AUDIENCE set answers GET /api/v1/diagrams with 200 [] and logs no `[ identity ]` line, while calling iapIdentity directly inside the same container logs `refusing assertions: no-assertion-header` immediately; live revision draw-00005-4jz shows 80 requests and 5 boot lines with no identity diagnostic]` | CLOSED -- H9.14. The audience is passed, and the incoherent combination is now impossible rather than merely fixed: `createApp` should refuse to start when `authz` is true and it has neither an `audience` nor an injected `principalOf`, because that state means "authorization is on and nobody can ever be anybody", which is never what a caller intends. A test must exercise the real `server.js` wiring, since injecting `principalOf` is what hid this. |
| **B71** | **An inbound `snapshot` mid-gesture silently destroys an in-flight route, because D12's defer rule covers `change` and not `snapshot`.** `input.js:388` cancels any live gesture when the model reports a `load`, and `GESTURES.link.cancel` at `:235-239` removes the preview path and calls `cleanupRoute`, which deletes every waypoint the user placed (`:748`). On the client `model.load` has exactly one caller: the `snapshot` branch of `Sync.onMessage` at `sync.js:214`. The deferral that exists precisely to stop this -- B19/D12, *a change arriving mid-gesture is queued, not applied under the preview* -- is implemented only in the `change` branch at `sync.js:280`, so a snapshot walks straight past it. Any of a lock handoff (`protocol.js:199`), a `requestResync` after an earlier rejection (`sync.js:295`), a `reclaim`, or a reconnect that is behind (`protocol.js:324`) will therefore erase a route the user is halfway through drawing, with no message. This is the reported symptom exactly: the link fails to connect and the path clears. It is also a recurrence of an already-mined lesson, which A14 signal 3 makes a fault in its own right -- D12 identified the hazard, fixed one transport of it, and the other was never covered. | `[V, app/src/input.js:388 `if (action === 'load' && this.mode) this.cancelDrag()`; input.js:235-239 cancel removes the path and calls cleanupRoute; input.js:748 deletes every placed waypoint; app/src/sync.js:214 is the only client caller of model.load; app/src/sync.js:280 the deferInbound guard exists only in the `change` branch; reproduced in a harness -- after `w`, mode=link with one placed waypoint, after an inbound snapshot mode=null with zero waypoints and zero commits]` | CLOSED -- H10.1. The defer rule now covers `snapshot`, and the cancel is untouched: a snapshot is authoritative and must land, it just must not land under a live gesture. Both sub-questions answered. A held snapshot **supersedes** held changes rather than being applied after them, because it is whole state and replaying deltas that predate it would put history on top of the present -- draining first would reach the same document only if every queued change were older, and nothing available at that point establishes it. Only **unsolicited** snapshots are held: one the user asked for carries `expectLoad`, and a gesture on the diagram they are leaving is moot, so holding it would make the app feel stuck rather than safe. The snapshot branch was extracted to `applySnapshot` so the release has somewhere to send a held message instead of re-entering `onMessage` past the guard it just tripped -- one method added, none removed, verified against HEAD. The second sub-question, what the user is TOLD, is deliberately not answered here: there is no durable surface to tell them on until B74 lands, and inventing a transient one that the next state emit overwrites would repeat the defect B74 describes. |
| **B72** | **Releasing a routed link onto a node already linked to the source silently discards the route and every waypoint placed for it.** `input.js:207` requires `!i.model.linkBetween(ctx.src.id, dst)`, and when that fails control reaches `:233` `i.cleanupRoute(ctx)`, which deletes the placed waypoints and leaves nothing behind. There is no message, no readout flash, and no record that anything was refused. The asymmetry is what makes it read as a waypoint bug rather than a duplicate-link rule: with no bend, the same rejected release falls through to the click-select branch at `:216` and the user simply sees a selection, whereas with a bend the user loses visible work they just created. So the identical rule produces a benign outcome in one case and silent destruction in the other, and the difference is exactly the thing the user pressed `w` to make. Confirmed by variant testing -- of nine scenarios driven through the real `Input`, only the pre-existing-link case produced zero commits with the waypoint discarded. | `[V, app/src/input.js:207 the `!linkBetween` precondition; input.js:233 cleanupRoute on the failing branch; input.js:216 the no-bend path falls through to click-select instead; input.js:747-749 cleanupRoute deletes every entry of ctx.placed; harness run over 9 variants -- basic w, w twice, w with no prior move, w then release on empty canvas, w on the destination cell, pre-existing link, read-only, digit 7, shift-at-release -- only the pre-existing-link variant lost the work]` | CLOSED -- H10.4. **RULED 2026-08-21: allow the second link.** The director's reasoning is the discriminator and narrows the change: *by using a waypoint between the nodes the paths do not collide and can fan out*. So the permission is grounded in the route, not in the pair. A second STRAIGHT link between the same two nodes renders exactly on top of the first, is invisible, and is indistinguishable from the no-op the user already gets -- allowing it would trade a silent discard for a silent duplicate. The reading taken is therefore: permit a duplicate when the new link carries a route, keep refusing a bare one. Flagged explicitly because it is an interpretation rather than a transcription, and is cheap to reverse if the intent was the literal reading. Scope is client-only and smaller than it looked: the server never refuses duplicates, `linkBetween` appears in no server file. Three call sites, and they do not all move together -- `input.js:207` the routed drag (change), `input.js:156` the endpoint re-drag which may carry a route (change), and `commands.js:227` `linkNodes` behind the `L` key which chains straight links in a batch (keep skipping, since every link it authors would collide). The tested behaviour at `tests/input.test.js` *L skips a pair that is already linked* stays true; the drag behaviour is what changed. Both drag sites moved and `linkNodes` did not, as planned. Confirmed in use: the director reports the waypoint route now completes, which was B71, and that the single-link restriction remained, which was this. **Confirmed by the director 2026-08-21:** the interpretation is right and there is a designed end state behind it -- up to three parallel links between a pair, as far as space permits -- which is concept only and has never been implemented, with a single straight link permitted for now. So this row delivers the routed case and the spacing rule is a separate, larger piece of work, now H10.7. **Located after all, and in the live repo the whole time** -- `design/walk/FINDINGS.md:19-23` and `:37-42`, where the rung `3-parallel3` runs three connections between two facing zone edges across three columns and the count cap is `cols - 1` for node-agnostic styles against `cols` for column-based ones, which is what *as space permits* means. Two corrections follow. The design is container-to-container rather than node-to-node, so it is adjacent to this row rather than a superset of it. And the first search missed it because it used *parallel link* and *same pair* where the corpus says *parallel* and *count* -- the corpus was not at fault. Separately B79: the findings survived and their generators did not. |
| **B73** | **The help overlay promises `7` as a mid-drag routing pivot; the keymap filters it out mid-drag and the handler refuses anyway.** `app/index.html:28` reads *"w / waypoint (7) -- routing pivot: bend a link (held-drag) or single-use link end"*, but the `hand` rule at `keymap.js:91` omits `duringGesture`, so it defaults false and `resolveKey` drops it while a gesture is live (`keymap.js:133`). Even if it reached the handler, `input.js:1100-1101` `onHandDigit` begins `if (this.mode) return`. Pressing `7` mid-link-drag therefore produces a straight link with no bend and no error -- the documented behaviour and the actual behaviour disagree, silently, on a key the help text explicitly pairs with one that does work. Small, and worth filing rather than fixing in passing because GR10 already enforces that documented commands exist and this is the same class of drift in a surface GR10 does not cover. | `[V, app/index.html:28 the help text pairing w and 7; app/src/keymap.js:88 the waypoint rule carries duringGesture: true; keymap.js:91 the hand rule does not; keymap.js:133 `if (ctx.gesturing && !r.duringGesture) continue`; app/src/input.js:1100-1101 onHandDigit returns immediately when this.mode is set; harness: digit 7 mid-drag yields one commit labelled `link` with no via]` | OPEN -- decide which side is wrong. If `7` is meant to work mid-drag it needs `duringGesture` and the guard removed; if it is not, the help text should stop promising it. The second is almost certainly right, since `w` already covers the case. |
| **B74** | **There is no durable surface for server messages, and the one transient surface is overwritten by the next state emit.** A server `error` reaches `sync.js:300` as `emitState({ error, code })` and is rendered at `main.js:130` into `#banner` -- but `main.js:105` unconditionally rewrites that same element with entity counts earlier in the very same handler, and `emitState({})` is called from `onStatus`, every `ack`, every `change`, `releaseDeferred`, both `applyLock` paths, `rename`, and `setSlidesUrl`. A rejection therefore survives until the next websocket message, which in an active session is milliseconds. The plumbed `code` is never rendered at all: `sync.js:300` sends it and `main.js:88` destructures only `error`. Nothing other than `cmd === 'error'` reaches the UI in any form, so there is no answer to *what did the server last tell me*. The footer has the space -- `#status` at `app/index.html:86` contains exactly one left-aligned child and no right-hand element, where the header has an explicit `#menu-spacer` for the same purpose. This is the client-side twin of B68: a system that refuses correctly and says nothing observable, and it is why B71 and B72 both present as inexplicable rather than as refusals. | `[V, app/src/sync.js:285-301 the error branch, :300 emits error and code; app/src/main.js:105 rewrites menu.banner with counts before :130 writes the error into the same element; main.js:88 destructures error and not code; app/index.html:86 footer #status has one child; app/style.css:146-147 no spacer; app/src/readout.js:177-182 picks one string by priority and writes it to every mount, so the existing readout cannot host a second channel without fighting the gesture text]` | OPEN -- a right-aligned element in `#status` is the slot that already exists. The readout is deliberately not the host: `readout.js:177` resolves one string for all mounts, so server messages and gesture coordinates would contend for it. Worth deciding at the same time whether this shows only errors or the last event of any kind, since the second is what makes a silent refusal visible. |
| **B75** | **Right-click is suppressed on the canvas only, so the browser menu opens over the top and left panels.** There is exactly one `contextmenu` handler in the repository -- `app/src/input.js:421` on the element `main.js:23` resolves as `#container`, the SVG canvas. `#menu` (`app/index.html:9-21`), `#palette` (`:69`), `#status` (`:86`), `#stage` (`:70`), `#help` (`:22`) and `body` have none. This is not a cosmetic inconsistency in this application specifically, because the right button is a first-class gesture button here -- `input.js:5` documents right-drag as move, and `recognize.js:50-52` binds `chord`, `r-clone` and `r-press` to it -- so a user's hand is already on the right button when it crosses onto the palette, which sits immediately adjacent to the canvas. `Palette.onDown` calls `preventDefault` at `palette.js:169`, but that suppresses the pointerdown default, not the context menu, so the native menu still opens. | `[V, exhaustive grep for contextmenu across app/ server/ tests/ docs/ public/ returns app/src/input.js:421 and nothing else; app/src/main.js:23 svg = getElementById('container'); app/index.html:71 the svg element; app/index.html:9,69,86,70,22 the unsuppressed regions; app/src/input.js:5 and app/src/recognize.js:50-52 establish the right button as a gesture button; app/src/palette.js:169 preventDefault on pointerdown only]` | CLOSED -- H10.5, and the disposition's own reasoning is what shipped: suppress everywhere except form fields, not a blanket handler. Moved from the svg to the injected `host`, which is the surface that already owns global events (B45), so the scope matches the problem. The exemption is the target's nearest form field rather than a list of ids -- naming elements is how this broke in the first place, and the application has since grown two more text inputs in the access panel that nobody would have remembered to add. Reported again from live use before this was picked up, which is the honest measure of how visible it was. |
| **B76** | **The client has no way to know who the signed-in user is; no identity reaches the browser at all.** `Session.principal` exists at `protocol.js:158` and `app.js:192` resolves a principal for every HTTP request, but no payload carries it: `snapshotBody` (`protocol.js:37-70`) consumes the principal for `canRead`, `list` and `canWrite` and returns none of it, and `syncBody`, `changeBody`, `reversalBody` and `ackBody` likewise carry no identity. `changeBody` carries `actor`, which is a per-session id minted at `protocol.js:156` and deliberately anonymous. There is no `/me` route in `server/rest.js`, and `/health` returns counts only. The near-miss makes the gap sharper: `snapshotBody` ships the whole document, so the browser DOES hold `meta.owner` and the full `meta.grants` map at `window.draw.model.state.meta` -- it can enumerate every principal with access to the diagram and cannot tell which one it is. `mayWrite` answers *what may I do* and was never intended to answer *who am I*. This blocks more than a label: any sharing UI, any "you are viewing as", and the connection-code surface at H9.5/H9.6 all need the client to know its own principal, and none of them can be built until one field exists. | `[V, server/protocol.js:37-70 snapshotBody returns doc, diagrams, locked, mayWrite, version, canUndo, canRedo, truncated, truncatedHuman, undoTop and no principal; protocol.js:156 actor is `s-${randomUUID().slice(0,8)}`; protocol.js:158 this.principal is set and never serialised; server/rest.js has no /me route and /health at :214-224 returns status, diagrams, flushFailures, invariantFailures; grep for principal, owner, grants, email across app/src and app/index.html returns zero functional references; model/model.mjs:54 meta carries owner and grants, and main.js:212 exposes the model on window]` | CLOSED -- H10.2. One field, `principal`, on `snapshotBody`; the principal and not an email, so the browser never parses an identity and `user:` stays distinguishable from `code:`. Null when authorization is off, because there is no principal then and inventing one would make a single-tenant run look multi-tenant. Placement resolved to the top right, immediately left of `#lockstate`: top left is where branding conventionally lives and an address there reads as though the application is named after the account, whereas identity beside the authority pill pairs the two questions a person has on arriving -- who am I, and what may I do here. The address is shown in full rather than abbreviated, because in a tool where one account sees a diagram and another does not, being certain WHICH account you are in is the entire value of the element; the `user:` prefix is stripped as protocol vocabulary and the untouched principal stays in the tooltip. `#logo` was left alone -- replacing branding is a separate decision and not one to take in passing. |
| **B77** | **`scan-board` verifies the board's structure and cannot see that its content is wrong.** The scanner checks that every citation resolves to a live `B` row, that every milestone named by an item exists, and that DONE and CLOSED agree -- and it passed continuously while four separate content defects sat in the file. The H9 header read `TODO` with 13 of 18 items DONE, so the milestone a reader takes first presented as not started. H9.10 read *"the 11 unowned diagrams"* when there are 12, a wrong number carried through the board, the backlog, and several session summaries before the bucket was counted. The production cutover -- build, deploy with `IAP_AUDIENCE` and `OWNER`, adopt 12, verify through IAP -- happened with no board item at all. And a scanner proposed in conversation lived only in that conversation, which is the exact Insight Depreciation fault the board exists to prevent. All four were found by reading the board, not by running the gate. The pattern is the one that produced B62, B69 and roughly a dozen instances this session: a check reports a property nobody asked about, and its green result is read as evidence about a property nobody checked. | `[V, tools/scan-board.mjs reports `70 row(s), 60 citation(s), 10 milestone(s), 54 item(s)` and PASSes while docs/BOARD.md:376 read `## H9 -- access control · TODO` above 13 DONE items; docs/BOARD.md H9.10 read "the 11 unowned diagrams" against 12 objects verified in gs://diagrams.apnex.io; no board item existed for the cutover across revisions draw-00004-6ts through draw-00007-rrn; grep for scan-wiring across docs/ returned zero before H9.16 was added]` | CLOSED -- H9.18, two of the three. R5 requires every item row to declare a state and R6 requires a heading to agree with the states beneath it in both directions, over EVERY row rather than the citing subset. That subset was the reason R4 already existed and saw none of this: it iterates rows that cite a `B` row, so `H2.3` -- four cells where its table has five, prose sitting where its state belongs, citing nothing -- was outside its reach, and so was `H2` reading DONE above it. Implementing R5 surfaced that the board carries two legitimate conventions: H0-H5 and H7-H10 use a trailing Status column, H6's table is `# | Stage | Risk | Proof` and prefixes the Stage cell. Requiring a particular cell would have forced a rewrite of a milestone closed months ago, so the rule is that a row declares a state and the last token wins. **The third check is not closed and is not mechanizable as written** -- *a number must not contradict one the repo can compute* -- because the number that produced this row, the 11-versus-12 diagram count, describes a GCS bucket the repository cannot see. A tractable form would name specific computable claims rather than numbers in general, and it needs the convention decided first, so it stays open rather than being quietly declared done. |
| **B78** | **`scan-board` matches milestones with a single-digit pattern, so H10 and everything after it is silently unenforced.** Four regexes in `tools/scan-board.mjs` spell the milestone as `H\d`: `:52` parsing a BACKLOG disposition, `:57` collecting the milestone set, `:61` matching item rows as `H\d\.\d+`, and `:93` reading milestone state. `H\d\b` cannot match `H10` -- `H1` matches the first two characters and the `\b` then fails against the `0` -- so a milestone numbered ten or higher does not exist as far as the gate is concerned. Discovered by creating H10 with six items and watching `scan-board` report `10 milestone(s), 55 item(s)` and PASS, where 55 counts only the single item added to H9. The consequence is the opposite of a noisy failure: the BOARD-to-BACKLOG contract, which is the mechanism GR14 exists to enforce, silently stops applying to all new work at exactly the point the project grows past nine milestones, and it announces this by saying PASS. Worth separating from B77: that one is a check that was never written, this one is a check that exists, runs, and quietly narrows its own scope. | `[V, tools/scan-board.mjs:57 `/^##\s+(H\d)\b/gm`, and the same `H\d` at :52, :61, :93; node -e over the literal heading confirms `/^##\s+(H\d)\b/` returns false for `## H10 -- client surface and observability` and `H\d+` returns true; with H10 and six items present in docs/BOARD.md the scanner reported `77 row(s), 67 citation(s), 10 milestone(s), 55 item(s)` and PASSED, where 55 is 54 plus the one item added to H9 and excludes all six H10 items]` | CLOSED -- H9.19. `H\d` became `H\d+` in all four places, and the fix needs a test that a two-digit milestone is actually counted, since the failure mode here is undercounting rather than erroring. Directly strengthens H9.18, which was filed on the assumption that the scanner at least sees every item. |
| **B79** | **The connection-walk findings survived and their apparatus did not, so the vocabulary the findings reason in is undefined anywhere in the repository.** `design/walk/` contains exactly one file, `FINDINGS.md`, byte-identical to the copy at `~/taceng/drawv2-archive/design-generators/design/walk/FINDINGS.md`, where it sits beside the five modules that produced it: `rungs.mjs` (the eight scenarios), `styles.mjs` (the eight variants), `scene.mjs`, `run.mjs`, and `grc.mjs` (the checker). Only `grc.mjs` graduated, to `kernel/grc.mjs`, and it is live. The other four were dropped. The findings therefore adjudicate between `nodeAligned`, `gatewayCell`, `bus`, `ports`, `hybrid`, `ortho` and `edgeNode` across rungs named `3-parallel3`, `7-group-zone` and `8-group-group`, and not one of those names is defined in the live tree -- `nodeAligned` and `gatewayCell` return a single grep hit each, which is their own mention inside `FINDINGS.md`; `3-parallel3`, `styles.mjs` and `rungs.mjs` return zero. The consequence is not tidiness. The principle *node-agnostic gutters number `cols - 1`, so these styles cap parallel at N-1 between N-column zones* is the design authority for how many parallel connections a pair of containers may carry, it was used to rule on a live feature this session, and it cannot be re-run, re-rendered, or falsified from this repository. Nor can the recorded refutation of `outer-flank gutters`. `scan-docrefs` passes because the only apparatus path `FINDINGS.md` happens to spell is `grc.mjs`, which resolves to the graduated file; the four modules that vanished are referenced by style name rather than by path, so the scanner had nothing to check and reported green on a document whose entire vocabulary is missing. The allow entry's reason -- *the walk record is historical* -- is the load-bearing error: a record that governs a decision taken today is not historical, and that label is what let the apparatus be discarded without the loss being weighed. | `[V, ls design/walk/ returns FINDINGS.md only; ls of the archive path returns FINDINGS.md plus grc.mjs, rungs.mjs, run.mjs, scene.mjs, styles.mjs; diff -q reports the two FINDINGS.md identical; kernel/grc.mjs exists and is imported by kernel/index.mjs, kernel/geometry.mjs and tests/span.test.js; grep across the live tree excluding FINDINGS.md returns 0 hits for 3-parallel3, styles.mjs and rungs.mjs, and 1 self-hit each for nodeAligned and gatewayCell; tools/scan-docrefs.mjs:82 allows design/walk/FINDINGS.md:design/walk/grc.mjs with the reason "the walk record is historical; the checker graduated to kernel/grc.mjs"; design/walk/FINDINGS.md:19-23 and :37-42 carry the cols-1 cap and the outer-flank refutation; the archive rungs.mjs:23 defines 3-parallel3 as edge(z, 3) between two facing zone edges over three columns]` | OPEN -- three separable questions. Whether the four modules return, which is cheap and makes the findings reproducible. Whether the allow entry's reason is corrected, since it is currently false and is the sentence that authorised the loss. And whether a scanner should require that a document's own vocabulary resolves somewhere, which is the general form and the only one that prevents a recurrence -- `scan-docrefs` checks paths, and the terms a design document reasons in are not paths. |
| **B80** | **H10.4 refused a straight link whenever ANY link existed between the pair, so a direct link became impossible once a routed one was drawn; and it falsified the `(<=1) endpoint-pair link` assumption without correcting it.** Two defects from one change, both mine. The gate at `app/src/input.js` reads `const duplicate = i.model.linkBetween(ctx.src.id, dst)` and permits the commit on `(!duplicate || via.length)`, which keys the refusal on *any* existing link rather than on an existing *straight* one. The director's ruling is that a single straight direct link is permitted **in addition to** routed paths; what shipped permits it only *before* them, and the order in which a person happens to draw decides whether they can have a direct link at all. The same fault sits in the endpoint re-drag at `:156`. Underneath it, `model.linkBetween` is documented at `engine/relations.mjs:88` as *the (<=1) endpoint-pair link between a and b* and returns the first match from the incident set; that bound held before H10.4 and does not hold after it, so the method now answers *is there a link* correctly and *which link* arbitrarily. The data is not corrupted -- `incident` maps a node to a Set of link ids and is not keyed on the pair -- but a caller reading the comment would believe a uniqueness the model no longer provides, and the caller that needs to ask *is there a STRAIGHT link between these two* cannot express the question with the API as it stands. | `[V, app/src/input.js the `(!duplicate || via.length)` gate and `const duplicate = i.model.linkBetween(...)`; app/src/input.js:156 the same predicate in the endpoint re-drag; engine/relations.mjs:88 the comment `the (<=1) endpoint-pair link between a and b` and a loop returning the first match; model/model.mjs linkBetween falls back to `.find(...)`, also first-match; engine/relations.mjs incident is Map<id, Set<linkId>> and carries no pair key, so multiple links between a pair are stored correctly and merely reported ambiguously; director report: a straight second link between a pair is refused, and that is a defect]` | CLOSED -- H10.9. The predicate is now *does a STRAIGHT link already exist between this pair*, not *does any link*. That question needs `linksBetween(a, b)` returning all of them, which also gives the `(<=1)` comment somewhere honest to go: `linkBetween` keeps its meaning for callers that only ask whether a pair is connected, and the new plural is what a caller filters. Worth noting the shape of my error rather than only the line: the director's ruling named the permission and I encoded its complement as the refusal, which is a different rule and a narrower one. |
| **B81** | **The one-straight-link-per-pair rule is enforced only where a link is CREATED, so an existing link can still become a straight duplicate by transition.** H10.9 put the check in the two drag sites in `app/src/input.js`, which is the moment a link is authored. Nothing guards the moment a link *becomes* straight. Two paths reach that state. The authoritative one is the waypoint cascade at `server/txn.mjs:209`: deleting a waypoint emits `set link via: link.via.filter(w => w !== id)` for every link routed through it, and when that waypoint was the link's only bend the patch leaves `via: []` -- a straight link. If the pair already carries one, the two now render exactly on top of each other, which is the collision the whole rule exists to prevent. `app/src/commands.js:75` mirrors the same strip as a local projection, so the client reaches the state too and agrees with the server about it. The second path needs no waypoint at all: `set` on a link is a first-class op, so a commit over either transport can clear `via` directly. That second path is what makes this a defect in the *placement* of the rule rather than a missing case -- a constraint enforced at two call sites is a convention, and a caller that does not go through those sites is not bound by it. The adjacent code already shows the shape of an answer: three lines above, at `:205`, a waypoint that is a link's ENDPOINT causes the link to be DELETED rather than stripped, because a link that cannot survive the operation does not limp on in a degenerate form. | `[V, server/txn.mjs:203-213 the waypoint branch, :209 the via strip with no check on the resulting length; server/txn.mjs:205-207 the endpoint case deletes the link instead; app/src/commands.js:69-75 the client-side mirror, `const remaining = link.via.filter((w) => !deletedWaypoints.has(w))`; app/src/input.js carries the only two guards and both are on authoring; `set` is a first-class op in the transaction vocabulary so via can be cleared without deleting anything; director: refuse any new or existing link BECOMING a straight link, if a straight link exists]` | CLOSED -- H10.10. **RULED (b): delete the colliding link.** B80 was mine for encoding the complement of a permission as a refusal; the same risk is here. The two candidates are not the same product. **(a) Refuse the operation**: deleting the waypoint fails while the collision would occur, which is literal to the word *refuse* but surprising -- the user is told they may not delete a waypoint because of a link they were not thinking about. **(b) Delete the colliding link**: the waypoint goes, and the link that would have become a straight duplicate goes with it in the same undoable step, which matches `:205` exactly and keeps the document in a state the rule permits. **Director's direction on placement, adopted:** a sovereign, deduplicated transaction validator, with the limit resolved rather than hardcoded -- the intended end state being a capacity read from a node-kind config property adjustable at runtime by an operator or over the API, explicitly out of scope for now. So `model/invariants.mjs` is sovereign (imports nothing, and `model/` is the substrate both server and browser already depend on, so neither restates the rule) and `straightCapacity(model, a, b)` is a function rather than a constant so that introducing configuration is a change to one body rather than a hunt through call sites. The rule moved to the transaction validator as a document invariant -- *at most one straight link between any pair* -- because that is the only placement the `set` path cannot walk around, and it converts a convention observed by two call sites into a property of the model. |
| **B82** | **"A node belongs to at most one group" is enforced on `put` and not on `set`, and the resulting document means different things to the client and the server.** `server/txn.mjs:165-167` carries the rule with a comment recording why it moved out of the browser -- *the rule existed ONLY in the browser, so POST /groups admitted a node to two groups* -- but it lives inside `planPut`. `planSet` contains no group logic, and `validateMutation`'s group branch at `validate.js:153-157` only checks that members exist. So a `set` patching `members` walks straight past it, reachable over `PATCH /api/v1/diagrams/:id/groups/:gid` (`rest.js:422-427`) and any websocket `commit` carrying a `set`. This is the exact shape B81 had: a rule attached to one op kind rather than stated about the document. The consequence is worse than B81's, because the two peers then disagree about what the document says. The client maintains the relational index, and `engine/relations.mjs:43` declares group membership `kind: 'single'` -- one owner per key -- so `groupOf()` returns whichever group was written LAST. The server runs with `model.index === null` and falls back to `model/model.mjs:193`, `this.all('group').find(...)`, which returns the FIRST in collection order. `groupOf` drives selection expansion (`model.mjs:302-312`) and the renderer hull (`renderer.js:135`), so a click selects one thing in the browser and another on the server, and neither is wrong by its own reading. | `[V, server/txn.mjs:165-181 the stealing loop inside planPut; server/txn.mjs:190-200 planSet with no group handling; server/validate.js:153-157 checks member existence only; probe -- two groups, `set` adding a member owned by the other returns ok:true with ops `[{op:'set',kind:'group',id:'group-000002',patch:{members:[...,'node-000000']}}]`, after which groups holding node-000000 = ['group-000001','group-000002'], validateDoc returns null and violations() returns []; engine/relations.mjs:43 membership is a single-valued index; model/model.mjs:193 the server-side fallback is a first-match find]` | CLOSED -- H10.12. The statement is in `model/invariants.mjs` and the repair stayed where it is. `planPut`'s stealing is a remedy; `violations()` is the assertion, which is the separation B81's own header already argues for. Add "no entity is a member of two groups" and the split-brain becomes a refused transaction rather than a document two peers read differently. |
| **B83** | **Four cross-entity referential rules are each written twice inside `validate.js`, and `violations()` is consulted on the transaction path and nowhere else.** `server/validate.js` is described in its header as *shape, ranges, and referential integrity*. The shape half is correctly placed -- every predicate in `validateEntity` reads one entity's own keys. The referential half is written out twice: once incrementally against a live `Model` in `validateMutation`, once globally against a plain doc in `validateDoc`. Link endpoints exist (`:135-136` versus `:223`), no self-link (`:137` versus `:224`), `via` waypoints exist (`:139` versus `:228`), waypoint used in at most one role (`:142-151` versus `:225-232`), group members exist (`:153-157` versus `:233-237`). Two implementations, two error vocabularies, two complexity classes -- the incremental waypoint check rescans `model.all('link')` for every waypoint in every link mutation, which is a document-wide predicate wearing a per-mutation costume -- and nothing forcing the pair to agree. Divergence means a document the wire refuses can be loaded from disk, or the reverse. Separately, `violations()` has exactly one consumer, `server/txn.mjs:94`. `Store.init`, `Store.create({doc})` and `#seedFromExamples` all reach `install` through `validateDoc` and never call it, so a whole document carrying the violation is admitted in silence. Tolerating it is right and argued at `txn.mjs:96-102`; never mentioning it is not the same thing, and the log invariant already shows the alternative -- `store.js:580-584` re-checks GR9 on every flush, counts into `invariantFailures`, and `/health` reports `corrupt` distinctly from `degraded`. | `[V, server/validate.js:135-157 and :223-237 the five rules stated twice; validate.js:144-151 scans model.all('link') per mutation; exhaustive grep for violations( returns model/invariants.mjs:53 and server/txn.mjs:34,94,103 only; server/store.js:100-128 init, :221-245 create and :164-182 seedFromExamples all call validateDoc and not violations; probe -- validateDoc(two straight links) returns null while violations() returns the violation; server/store.js:580-584 and server/rest.js:221-224 the existing invariantFailures channel]` | PART-CLOSED -- H10.13 did the cheap half. Document invariants now have the surface the log invariant already has: call `violations()` at load and creation, count into `invariantFailures`, report through `/health`, and do NOT make it a refusal. `Store.install` is the single choke point every whole-document path passes, so one call covers `init`, `create({doc})` and the example seed; a violation is counted into the existing `invariantFailures` and surfaces at `/health` as `corrupt`, and the document still opens. **Still open (H10.16):** collapsing the four twice-written rules into `violations()` and leaving `validateMutation` only the per-entity shape work; it is blocked on a real design question rather than effort, since `violations(model)` reads `model.all(...)` while `validateDoc(doc)` holds arrays, and `model/model.mjs:29 projection()` is the precedent for either answer. |
| **B84** | **The capacity moved into the sovereign module and the vocabulary the rule is written in did not.** `model/invariants.mjs:20-21` states that the rule has one home *so neither side restates it*, and that is true of `straightCapacity`. It is not true of the two predicates the rule is expressed with. `pairKey` is hand-written three times, identically, at `invariants.mjs:60`, `server/txn.mjs:245` and `app/src/commands.js:80`. *Is this link straight* has six spellings, three of them negated: `invariants.mjs:44`, `txn.mjs:254`, `commands.js:83`, `commands.js:256`, `input.js:157`, `input.js:159` and `input.js:228`. Both helpers are private to the module that owns the rule, so every caller re-derives them, and re-deriving a predicate slightly differently is precisely the failure B81 was filed for. The audit also found the client-side comment at `commands.js:76-78` claims more than holds: *the server is authoritative and its planner refuses the state outright, so a client that guessed wrong would see its optimistic view corrected*. That is true in one direction only. A client that guesses `strip` where the server would `delete` is refused and resyncs. A client that guesses `delete` where the server would `strip` is accepted, because deleting can never introduce a violation, and the user silently loses a link the server would have kept. The two `straightPairs` computations differ in what they exclude -- `deletedLinks` at `commands.js:83` is a superset of `dying` at `txn.mjs:249` -- so they are structurally capable of disagreeing. | `[V, pairKey at model/invariants.mjs:60, server/txn.mjs:245, app/src/commands.js:80; straightness spelled at model/invariants.mjs:44, server/txn.mjs:254, app/src/commands.js:83 and :256, app/src/input.js:157, :159, :228; app/src/commands.js:76-78 the projection claim; app/src/commands.js:83 excludes deletedLinks where server/txn.mjs:249 excludes only links whose endpoint is this waypoint]` | CLOSED -- H10.14. `isStraight` and `pairKey` are exported and the call sites rewritten against them, which is two `export` keywords and removes the only thing five call sites can get wrong. The one-directional hazard in the cascade pair is stated as structurally possible and UNTESTED rather than demonstrated: the auditor could not construct a divergence because the server plans sequentially against an advancing projection and usually sees the same world. Worth a test that asserts the two agree over the GR6 corpus rather than a fix in the dark. |
| **B85** | **"A group requires at least two members" is a browser-only rule, and a group may hold the same member twice.** `app/src/commands.js:127-128` refuses to build a group below two members, and `engine/policy.mjs:13-15` owns the dissolve threshold and is correctly shared by both peers. But the standing property -- a group in the document has at least two distinct members -- exists nowhere on the server. `server/validate.js:86` checks only that `members` is an array of at most 500 valid ids. A one-member and a zero-member group both plan `ok`, reachable through `POST /api/v1/diagrams/:id/groups` (`rest.js:90`, `:414-420`) with no minimum anywhere in the chain, which is the same hole `txn.mjs:165` already records having fixed for the two-group case. Duplicates are unchecked in the same place: a group with `members: ['node-000000','node-000000']` is accepted, `groupAfterRemoval` filters so removing *one* copy leaves the other, and the single-valued index at `engine/ivm.mjs:32` collapses them. A third threshold appears at `app/src/commands.js:421`, which uses `members.length > 0` when cloning. | `[V, app/src/commands.js:127-128 the client minimum; engine/policy.mjs:13-15 the shared dissolve threshold; server/validate.js:86 the only server-side group check; probe -- PUT of a 1-member group returns ok:true and PUT of a 0-member group returns ok:true; probe -- PUT of a group with a duplicated member returns ok:true; app/src/commands.js:421 a third threshold of > 0]` | CLOSED -- H10.15. One predicate loop in `violations()` covers both the minimum and the duplicate, and it is the same family as B82. Worth doing in the same pass, since the proof harness is identical. |
| **B86** | **Constants restated rather than shared, in eight places, two of them with comments claiming the opposite.** `MAX_COLLECTION` is `2000` at `server/txn.mjs:37` and a bare literal `2000` at `server/validate.js:209`, so changing the cap in one place makes load accept what commit refuses. `OPTIONAL` is exported from `model/shape.mjs:32-38` whose header at `:11` claims it replaced *the OPTIONAL map in server/validate.js* -- that map is still at `validate.js:93` and is still the one consulted at `:107`, while `server/txn.mjs:31` imports the `shape.mjs` version and never uses it, a dead import that makes the tree look single-sourced. `SELECTABLE` is a Set at `model/model.mjs:13` and a regex at `server/validate.js:13`, with a comment at `model.mjs:13` reading *MUST match server/validate.js SELECTABLE*, which is a comment doing the job of a check. The name cap of 64 appears at `validate.js:55,77,85,168,269`, `store.js:53`, `protocol.js:265`, `sync.js:491` and `labeledit.js:10` with no shared constant, and the two responses differ -- `store.js:53` and `protocol.js:265` TRUNCATE where `validate.js:168` REJECTS, which is not currently a bug because truncation happens first, and is the kind of pair that becomes one. The Slides URL cap of 512 appears five times, the content value cap of 256 twice, and the zone minimum three times as a bare `60` on the server against `MIN_ZONE = GAP` derived from `kernel/spec.mjs` on the client. | `[V, server/txn.mjs:37 MAX_COLLECTION versus server/validate.js:209 literal 2000; model/shape.mjs:11 the superseded-by claim, model/shape.mjs:32-38 the export, server/validate.js:93 the surviving map, server/validate.js:107 the consulting site, server/txn.mjs:31 the unused import; model/model.mjs:13 and server/validate.js:13 SELECTABLE with the matching comment; the 64 cap at server/validate.js:55,77,85,168,269 and server/store.js:53 and server/protocol.js:265 and app/src/sync.js:491 and app/src/labeledit.js:10; server/validate.js:80-81 literal 60 versus app/src/snap.js:69 MIN_ZONE = GAP]` | OPEN -- cleanup tier, and cheap. `MAX_COLLECTION` and the dead `OPTIONAL` import are one line each and remove a live inconsistency between what loads and what commits. The rest is bookkeeping. Deliberately NOT included: `validate.js:18`, where the EXT magnitude comes from `model/surface.mjs` but the check stays local because the trust boundary is never delegated -- that duplication is argued in the file and is correct. |
| **B87** | **The B81 client cascade emits a `del` entry in the wrong shape, so deleting a waypoint that would leave a colliding link THROWS in the browser -- and it is deployed.** `app/src/commands.js:91` pushes `{ op: 'del', kind: 'link', id: link.id }`. The file's own header at `:6` documents the shape as `{ op: 'del', kind, entity }` -- *the entity rides along for the local apply* -- and every one of the eight other `del` entries in the file uses `entity: clone(kind, ...)`. `Changes.commit` converts entries with `toOp` at `app/src/changes.js:24`, which reads `entry.entity.id`, so the missing field is not a cosmetic inconsistency: the conversion throws `TypeError: Cannot read properties of undefined (reading 'id')` before any op reaches the wire, and the local apply has no entity to remove either. The gesture therefore fails outright in exactly the case H10.10 was written to handle. How it got here is the part worth recording. I first wrote `before: { ...link }`, `scan-writers` rejected it under B44 -- builders emit forward intent only, the server derives the inverse -- and I removed the field rather than correcting it to `entity`. The scanner was right about `before` and said nothing about `entity`, because it checks for a forbidden key and not for a required one, so a correct rejection led to an incorrect fix. No test caught it because no test drives the client cascade through `toOp` for this case: `tests/commands.test.js` asserts over `entries` directly, and the one test that converts entries to ops uses a different command. Shipped in `0250cd1`, live as revision `draw-00010-gws`. | `[V, app/src/commands.js:91 the entry with id and no entity; app/src/commands.js:6 the documented shape; app/src/commands.js:105,114,116,117,119,134,190 the eight sibling del entries, all carrying entity: clone(...); app/src/changes.js:24 `if (entry.op === 'del') return { op: 'del', kind: entry.kind, id: entry.entity.id }`; probe -- deleteSelection over a pair holding one straight and one routed link, deleting the only bend, returns entries [{op:del,kind:link,hasEntity:false},{op:del,kind:waypoint,hasEntity:true}] and mapping them through toOp throws TypeError: Cannot read properties of undefined (reading 'id')]` | CLOSED -- H10.14. `id: link.id` became `entity: clone('link', link)`. The test that should exist is the one whose absence allowed it: drive the client cascade through the real `toOp` for every branch it can emit, not over the entries alone. Worth noting for `scan-writers` as well -- it enforces that no entry carries `before` and does not enforce that every entry carries what its op requires, which is the asymmetry that turned a correct rejection into a broken fix. |
| **B88** | **`H9.18` checks that a milestone heading agrees with the items beneath it and does not look at prose, so a paragraph asserting that finished work is blocked passes the gate.** The `Decisions required` section of `docs/BOARD.md` opened with *Four, all in H0.4. Each blocks work that should not start until it is answered* and then named three blockers -- **Blocks H5.3**, **Blocks H2.5**, **Blocks H2.3** -- every one of which was `DONE`. Two of the decisions had been answered by later work that never returned to amend the section: `H2.5` was retired under X15 with the reason recorded in its own row, and `H2.3` was re-ruled at H7 when `.github/workflows/gate.yml` landed, directly contradicting the section's claim that *a fresh clone has neither*. The third was answered by fact rather than ruling -- `server/svg.mjs:17` renders every SVG export through `docToSchema`, and the clone the decision was really about had already been removed. This is the same drift class as B77, and it survived the check written for B77 the same morning: R6 compares a heading's declared state against the states of the item ROWS beneath it, and a prose section is neither. So the board can assert, in sentences a reader trusts more than a table, that finished work is blocked. Found by reading the one item flagged as untouched all day, not by the gate. | `[V, docs/BOARD.md Decisions required, pre-correction: "Four, all in H0.4. Each blocks work that should not start until it is answered", with **Blocks H5.3**, **Blocks H2.5**, **Blocks H2.3**; docs/BOARD.md H2.3 reads "Re-ruled at H7: CI EXISTS" and is DONE; H2.5 reads "Retired (X15)" and is DONE; H5.3 is DONE; .github/workflows/gate.yml exists; server/svg.mjs:17 `const body = render(docToSchema(doc))`; app/src/renderer.js:10 imports contentLayout from the kernel and :30 records the layout/emission split; tools/scan-board.mjs R6 iterates allRows built from lines matching `^\|\s*(H\d+\.\d+)\s*\|` and reads no prose]` | OPEN -- the tractable form is narrow and worth doing precisely because the general form is not: a claim of the shape *blocks Hn.m* is a fixed phrase, and a board that says one thing blocks another can be checked against that item's state without parsing English. Anything broader -- prose that contradicts a row without naming it in a recognised pattern -- is not mechanizable, and pretending otherwise would repeat the H9.20 mistake of writing a check that cannot cover the case that produced it. |
| **B89** | **Two specification documents assert facts the implementation contradicted, one of them about a security boundary.** `docs/spec/DEPLOY.md:283` read *`/d/<id>.svg` is unauthenticated by nature today: a self-contained image at a URL*. That stopped being true at B67/H10.12, when reads were gated and the SVG route joined them -- an SVG is a rendering of the whole document, so leaving it open was never a property of images. A reader planning an embedding feature, or auditing what is exposed, would have taken the sentence at face value and been wrong in the direction that matters. `docs/spec/SCOPE.md:144` read *enforcement is H9.3 and not yet built*, which stopped being true at H9.3a and is now wrong three times over: writes are gated at all seven mutating store methods, reads at `hello`, `open`, the REST document and log and the SVG, and lock acquisition and reclaim at H9.4. Both are the class B88 named -- prose asserting a state the repository contradicts -- but neither is reachable by B88's proposed check, which reads *blocks Hn.m* claims on the board. One contradicts the CODE and the other contradicts the BOARD, and no scanner reads either against prose. Found by auditing for staleness after a day of change, not by the gate. | `[V, docs/spec/DEPLOY.md:283 pre-correction "unauthenticated by nature today"; server/app.js the SVG route now returns 403 when `store.canRead` is false, asserted by tests/access.test.js "B67: REST document, history and the SVG rendering are all gated"; docs/spec/SCOPE.md:144 pre-correction "enforcement is H9.3 and not yet built"; docs/BOARD.md H9.3a, H9.3b, H9.3c and H9.4 are all DONE; tools/scan-docrefs.mjs checks that paths resolve and reads no claims]` | CLOSED -- both corrected in place with dated amendments rather than silent edits, because a reader who relied on the old sentence should be able to see that it changed and when. The general problem is not closed and is not cheaply closeable: a scanner that verified arbitrary prose against code would be a type checker for English. What IS tractable was already scoped as H10.20 for the *blocks Hn.m* pattern, and the honest position is that the rest of this class is caught by reading, which is why the periodic audit is the control rather than the gate. |
| **B90** | **Authorization is enforced everywhere and cannot be administered anywhere: no production code path can create a grant.** `Store#grant`, `Store#revoke` and `Store#setOwner` have no caller outside `tests/access.test.js`, and `setOwner` is reached in production only through `adopt()`, which runs once at boot for `OWNER`. So in the deployed system every diagram is owned by `user:aobersnel@apnex.com.au`, `meta.grants` is empty on all twelve, and no request, command or CLI verb can change either. H9 built the whole enforcement half -- seven mutating methods, five read paths, both lock routes -- of a model whose only reachable state is *one person owns everything*. The consequence for the next item is direct: H9.4c would add `grantOwner` and `revokeOwner` beside two methods that already have no callers, leaving the `access()` fallback unreachable in production and provable only by test, which is the "proven reachable" rather than "proven to bite" distinction X13 exists to refuse. | `[V, server/store.js:517 grant, :528 revoke, :508 setOwner; whole-repo search for the call spellings returns 29 sites, all in tests/access.test.js and none under server/, cli/ or app/; server/store.js:502 is adopt's only setOwner call; observed in the live bucket 2026-08-21, all 12 objects in gs://diagrams.apnex.io carry owner=user:aobersnel@apnex.com.au and grants={}, so the inferred state is the actual one]` | CLOSED -- H9.4d. `POST /api/v1/diagrams/:id/grants` and `DELETE .../grants/:principal`, owner-only, not lock-gated, consumed by an access panel in the browser -- which needs no service account and no second OAuth client, so administration is not tied to one identity provider. No GET was added: `model.toJSON()` already carries owner and grants to every reader. Exposing the surface also surfaced a latent corruption path, now closed: `store.grant` never validated the principal while `validateDoc` does, so a malformed grant would have persisted and made the diagram refuse to load at the next boot. |
| **B91** | **`scan-dead` reports "every export has a production consumer" while examining only module exports, so an unreachable method of an exported class passes the gate.** The header states the rule as "a real consumer outside its origin" and the PASS line generalises it to "every export", but the scanner resolves `export` keywords, and `grant`, `revoke` and `setOwner` are members of an exported class. `Store` has consumers, so every method it carries is counted as reached whether or not anything calls it. This is how B90 survived the entire H9 arc with the gate green: the scanner's scope is narrower than its stated claim, which is the defect it was itself repaired for at H9.11, where internal use was being counted as consumption. The cheap fix is not obviously the right one -- widening to public methods of exported classes will surface a large first run, and narrowing the sentence to what is actually checked is honest but buys nothing. | `[V, tools/scan-dead.mjs:6 states the rule as a real consumer outside its origin; tools/scan-dead.mjs:153 is the PASS line; server/store.js:517 grant is a method of the exported class Store and scan-dead reports no finding against it]` | CLOSED -- H9.23, and NOT by widening the export rule, which was the obvious move and is wrong. Measured first: applying "a consumer outside its origin" to methods reports 118 of 293, because `this.onKeyDown()` inside its own class is how a method is normally used -- for an export the module is the boundary, for a method the class is. The rule that means something is that NOTHING calls it, origin included, which is exactly B90's shape and yields one finding. Scoped to server/model/engine/kernel and the scope is stated in the PASS line, because the client dispatches key handlers by name from the KEYMAP table and no text-derived call graph can see that -- including it would report thirty-two live handlers as dead. First run found `Store#patchMeta`, deleted at H9.23: superseded by `planMeta`, and a bypass of `commit()` that would have given a future caller a rename neither undoable nor broadcast. |
| **B92** | **`scan-board` cannot see a board item with a lettered suffix, so nine rows -- seven of them DONE -- were exempt from every rule the scanner enforces.** The item pattern required the id cell to end at the digits, while the board's own convention splits an item into `H9.2a`, `H9.2b`, `H9.2c`, `H9.3a`, `H9.3b`, `H9.3c`, `H9.4b`, `H9.4c` and `H9.4d`. Every one is in H9, the milestone under active work. The consequences run through all four rules that read item rows: R1 never resolved their citations, R3 never checked that a DONE lettered item has a CLOSED B row -- the contract rule the scanner's own header calls the one that silently rots -- and R5 and R6 judged the H9 heading against a subset missing seven DONE and two TODO rows. This is B78 again in the same file: that defect was a single-digit milestone class unable to match H10, fixed in four places at H9.19, and the item pattern beside it was never examined for the same narrowness. A second, smaller defect in the same line: the summary printed the count of rows that cite a B row under the name "item(s)", so the number a reader takes as the board's size was a subset whose name did not say so. | `[V, tools/scan-board.mjs:67 pre-fix the id group ended at the digit class; the board carries 9 item rows whose id cell has a letter suffix against 105 without; the summary printed 106 item(s) before the fix and 115 after, a difference of exactly those 9; tools/scan-board.mjs:64 items holds citing rows only while :175 printed items.length as "item(s)"]` | CLOSED -- H9.24. The pattern now admits the suffix and the summary names both numbers. Proven by mutant: reverting the pattern fails GR14/B92 and GR14/B78, and silently drops the real board from 115 items to 106. |
| **B93** | **The switch that turns authorization on is spelled as the presence of an IAP configuration value, so replacing the authentication mechanism silently disables the authorization model.** `canRead` and `canWrite` both return `true` when `authz` is false, which is correct for local development and catastrophic anywhere else, and `authz` is derived as `Boolean(audience)` from `IAP_AUDIENCE`. Authorization is therefore not an independent concept in this codebase: it is a shadow of whether one specific Google product is configured. This is fenced today, and only by one guard -- a boot refusal when a bucket is set without an audience -- which is itself written in the vocabulary of the mechanism being replaced, so the first natural step of an SSO migration ("IAP_AUDIENCE is obsolete, remove it") runs directly into the one thing standing between a misconfiguration and a production store where every diagram is readable and writable by anyone who reaches the port. The lexical coupling is already small and was measured rather than assumed: IAP-aware code is confined to `server/identity.mjs` and two lines of `server/app.js`, and every other mention across server/ is prose inside a doc comment. What is coupled is the concept, not the imports. Fix: authorization becomes its own switch, the boot guard demands an identity SOURCE rather than an IAP audience, and `identity.mjs` stays the only module that has heard of Google. | `[V, server/store.js:365 canRead returns true when !authz, :374 canWrite the same; server/server.js:50 audience comes from IAP_AUDIENCE, :68 authz: Boolean(audience), :55 the boot refusal keys on audience; server/app.js:152 the identity source keys on audience; measured coupling -- server/log.mjs 1 mention 0 in code, server/validate.js 1 and 1 in a doc comment, server/store.js 1 and 1 in a doc comment, server/app.js 4 and 3, server/identity.mjs 17 and 15]` | CLOSED -- H9.25. `identitySource()` in `server/identity.mjs` is now the only place that decides which mechanism is in play, `createApp` no longer takes an `audience` and `app.js` no longer imports `iapIdentity`, and both the switch and the boot guard are phrased in terms of a source. B70's shape is additionally unrepresentable rather than merely detected: with one identity argument there is no second value left to test in place of passing it. Proven by three mutants -- restoring Boolean(audience), restoring the app's import of the mechanism, and re-keying the boot guard on the product -- each of which fails a named test. |
| **B94** | **A revoked peer's open tab keeps believing it may write until it reconnects, because a grant change is not broadcast.** `mayWrite` is computed once per snapshot and a grant travels over neither the websocket nor any event, so a browser holding an older snapshot still renders an editable canvas after the owner has revoked it. This is a user-experience defect and NOT a security one, and the distinction is load-bearing: the server re-checks the grant on every mutating path, so the revoked peer's writes are refused with 403 whatever its tab believes. The fix is not simply broadcasting the new grant map, which is why it is deferred rather than bolted on: `mayWrite` is per-principal while `hub.broadcast` sends one body to every session on a diagram, so a single broadcast body would be wrong for somebody, and having each client recompute the predicate from a grants map would put a second copy of the rule in the browser -- the exact drift H9.3c removed by making the server the sole author of `mayWrite`. The shape that fits is a contentless signal that prompts each session to refresh its own snapshot. | `[V, server/protocol.js snapshotBody computes mayWrite per principal at snapshot time; server/rest.js the grant routes call neither hub.broadcast nor any per-session refresh; server/store.js grant and revoke only markDirty; tests/access.test.js H9.4d proves the server still refuses the revoked principal, so the gap is belief and not access]` | OPEN -- H10.22. |
| **B95** | **The `doc` / `Model` distinction is stated only in a closed backlog row's disposition cell, so the question of whether `validateDoc` is misnamed recurs and each recurrence risks a wrong rename.** H5.7 renamed the substrate `document/` to `model/` because one spelling covered three concepts, and ruled in the same commit that `doc`, `docfile` and `validateDoc` stay: a `doc` is the flat JSON that goes to disk and over the wire, a `Model` is the live object with methods, and `model.toJSON()` produces the former from the latter. The ruling is sound and the code still obeys it exactly -- all three `validateDoc` call sites receive `parse()` output and a Model has never reached it -- but the reasoning lives in a cell of a CLOSED row, which is the one place a reader of the code will not look. The recurrence is observed, not hypothetical: the question was asked again on 2026-08-21, five milestones after the decision. The cost is asymmetric, which is what makes this worth a row rather than a shrug: re-deriving the answer is a few minutes, and acting on the wrong answer renames a symbol with 38 call sites and destroys a distinction that is load-bearing, since validation applies ONLY at the serialization boundary and a `validateModel` would name something nothing does. | `[V, git e2d63d1 'document/ -> model/ -- one spelling no longer covers three concepts'; docs/BACKLOG.md B41 disposition records the doc/Model split; server/store.js:106, :171 and :247 are every validateDoc call site and each takes parse() output or a spread of it; no occurrence of the distinction in server/validate.js, model/model.mjs or docs/spec/SCOPE.md; SCOPE.md has no vocabulary section among its eleven headings]` | CLOSED -- H5.8. Stated at both sites a reader meets the names, and in a SCOPE.md vocabulary section, so the answer is where the question is asked. |
| **B96** | **GR10 asserts that every REST route the server answers is documented in README, while deriving routes only from collection names and sub-routes at ONE path position, so an entire new top-level route family landed undocumented and the gate stayed green.** The derivation reads the `COLLECTIONS` map and then every `parts[4] === '...'` branch, which covers sub-routes hanging off a diagram and nothing else. H9.4c added a workspace family whose branch tests `parts[2]`, and GR10 did not notice it existed. The test is the derived kind the repo prefers -- it is explicitly modelled on X9, and its own header says the REST surface is DERIVED from the router exactly as the websocket vocabulary is -- which is what makes the narrowness worth a row: a derived check that silently covers a subset is more dangerous than a token grep, because the header persuades a reader that the whole surface is held. This is the same defect as B78, B92 and B91 in different files, and the count is now four in one session: a check whose scope is narrower than the sentence describing it, with nobody comparing the sentence to the code. | `[V, tests/spec.test.js the GR10 route test derives from /const COLLECTIONS = / and from /parts.4. === '([a-z]+)'/ only; server/rest.js routes the workspace family on parts[2] === 'workspace' and parts[3] === 'grants'; the suite passed with the new routes present and README silent about them]` | CLOSED -- H9.26. The derivation now reads any path position rather than the fifth, which brings the workspace family and any future top-level route under the rule. Proven by mutant: removing the workspace lines from README fails GR10, where before the widening it passed. |
| **B97** | **README keeps three sections S4 would not allow, by ruling rather than by oversight.** S4 says the top-level README orients on four operator journeys -- install, use, test, remove -- and that apart from a one-paragraph intro and a prerequisites overview, nothing else should sit among them. This README also carries Diagram storage, Google Slides sync and Layout. The REST API was a fourth and was split out at H9.27, but on its own merits: it answers a different reader and was a third of the file. The director ruled on 2026-08-21 not to apply S4 strictly here, so the remaining three stay. This row exists only so that the decision is met by whoever next reads S4 against this file: without it the three sections read as an oversight, and the obvious helpful act is to delete them, which is re-litigating a ruling rather than finding a defect. B95 is the same shape -- a sound decision recorded only where nobody looks, re-derived five milestones later. | `[V, style/S4-four-journey-readme.md states the four journeys and the nothing-else rule; README.md carries Diagram storage, Google Slides sync and Layout beside them; director ruling 2026-08-21 in session]` | WON'T DO -- ruled. REVIVAL TRIGGER: the README passes 240 lines again, or a reader reports the four journeys are hard to find among the other sections. Then revisit which sections earn their place, on readability rather than on S4 compliance. |
| **B98** | **Nothing bounds how many diagrams can exist, and H9.21 is the first path on which a program rather than a person creates them.** `MAX_COLLECTION` caps entities per kind per diagram and says so in its own comment -- it is explicitly a different cap -- and no equivalent exists for the store itself. That was tolerable while creation required either a human pressing the header button or an open websocket session, because both are paced by a person. A REST `POST` is not: a retry loop around a call that looks like it failed, or an agent with a mistaken termination condition, creates diagrams as fast as the backend accepts writes. The cost is not only storage. Every diagram becomes an object in the bucket, `list()` walks all of them on every diagram listing, and `init()` reads and validates every one at boot, so an unbounded store turns into a slow boot and then a failed one, on a service running with minScale=1 where a boot failure is an outage. Registered rather than assumed away because the capability being added is exactly the one that changes the risk. | `[V, server/txn.mjs:37 MAX_COLLECTION is per KIND per diagram and the comment says a different cap; no MAX_DIAGRAMS or equivalent anywhere in server/; server/store.js:115 init() iterates every file the backend lists and validates each; server/store.js create() has no count check]` | CLOSED -- H9.21. A store-level cap lands with the route that made it reachable, configurable by MAX_DIAGRAMS and defaulting generously, refusing before anything is written. |
| **B99** | **An agent name is global while a code mints against it, so without a claim rule anyone who can mint impersonates any agent.** ACCESS.md specifies the code's shape, storage, transport and expiry and never says who may mint one FOR a given agent. `agent:planner` is a bare name in the principal grammar with no owner, so if two people may mint, the second obtains a credential that authenticates as the identity the first granted access to -- a privilege escalation that needs no defect in any check, only the absence of a rule. It is theoretical in this deployment and only by accident: IAP admits exactly one member, so there is one person who can mint anything. It stops being theoretical the moment a second person is admitted, which is a one-line IAM change nobody would connect to this. Recorded before building rather than after, because a credential system that permits name-squatting is not a defect to be patched later -- the codes minted under it stay valid. | `[V, docs/spec/ACCESS.md lines 24-27 give shape, storage, transport and expiry with no minting authority; search for who-may-mint, claim, impersonate and squat across ACCESS.md returns nothing on the subject; server/validate.js the agent grammar is a bare name carrying no owner; gcloud iap web get-iam-policy lists exactly one member]` | CLOSED -- H9.5. First mint CLAIMS the agent name for the minting principal, and only the claimant may mint against it afterwards. The claim is recorded beside the code rather than in the principal grammar, so `agent:<name>` stays a bare durable identity as H9.4b ruled and nothing about grants changes. |
| **B100** | **An agent creates a diagram and the human who authorised the agent cannot see it, so agent-first authoring produces work nobody can reach.** ACCESS.md predicted exactly this and only half of it was built: *if an agent may create, the human needs access to what it creates and the agent needs access to what the human creates*. H9.4c built the second half -- a workspace grant from the human reaching everything the human owns -- and nothing reaches the other way. A diagram created over `POST /connect/v1/diagrams` is owned by `agent:planner` with empty grants, so `list()` filters it out of the human's listing, `canRead` refuses the document, and the SVG route refuses too. The director could not see a diagram drawn on their own deployment, by an agent they had themselves granted, in a bucket they own. The reported second symptom -- *no edits were visible and the lock indicator never moved* -- is DOWNSTREAM of this and not a defect in the fan-out: broadcast is correctly wired for REST commits and for the lock, and it is scoped per diagram by design, so a viewer who cannot open the diagram was never going to receive its events. Recording that attribution matters as much as the defect, because *edits do not broadcast* would send the next reader into the wrong file. | `[V, gs://diagrams.apnex.io/diagram-f35a46.json has owner agent:planner and grants {}; server/store.js:567 list() filters by access() when authz is on; server/rest.js:191 and :596 broadcast change on a REST commit and :484/:495 broadcast lock, so the machinery is present; docs/spec/ACCESS.md states the reciprocity requirement in the section lifting collection scope]` | FIXED -- H9.30. Ruled 2026-08-23 as OWNERSHIP, not a reciprocal grant: `ownerFor(principal)` resolves an agent to its claimant, so what an agent creates is owned by whoever claimed the agent name. The agent is left an ordinary `write` grant on its own work, because relying on the human's workspace grant would let an agent holding a code but no workspace grant create a diagram and lose it in the same call. The reason for ownership over a grant is the one the tests are written against: a grant leaves the AGENT owning the diagram and so holding its access list, meaning the human could neither grant anyone else nor revoke the agent from work done on their behalf -- the instrument outranking the person who authorised it. `claimantOf` is never null for an authenticated agent because `agentForCode` refuses a code whose agent has no live claim, so the resolution is always defined. Both create paths were routed through one private `#attribute`, since the doc-carrying one set the owner on its own line and would have been the one to miss a change. Four mutants bite. API.md previously stated the caller owns what it creates and now carries a dated amendment; ACCESS.md records that both halves of its reciprocity requirement are built. One existing diagram, `diagram-f35a46`, predates the fix and is corrected out of band -- one object, so a permanent migration would be dead weight for a condition that can no longer arise. CONFIRMED live by the director: `agent-dogfood` appears in their list, which is the symptom the row opened with. The agent half was checked separately -- it can still lock and write the diagram it no longer owns. |
| **B101** | **An agent cannot obtain the rendered picture of the diagram it just drew.** `/d/<id>.svg` is a top-level route and the load balancer sends only `/connect/*` to the backend without IAP, so a bearer-authenticated caller receives `401 Invalid IAP credentials` from IAP rather than an image. Neither `/connect/d/<id>.svg` nor `/connect/v1/d/<id>.svg` resolves, because the rewrite maps `/connect/v1` onto `/api/v1` and the SVG route lives beneath neither. The consequence is not cosmetic: the agent cannot check its own work visually, and cannot hand a human a picture of what it built, which is the natural way to report a drawing. | `[V, GET https://draw.apnex.io/d/diagram-f35a46.svg with a valid bearer code returns 401 with body 'Invalid IAP credentials: Unable to parse JWT'; /connect/d/... and /connect/v1/d/... both 404; the same document over /connect/v1/diagrams/<id> returns 200]` | FIXED -- H9.31, and the director ruled the shape: `/connect` is the FRONT DOOR, so the picture is reached by naming the editor's own route without the prefix. The alternative was a new `/api/v1/diagrams/<id>/svg`, which would have been cheaper and left two routes rendering one thing. The door moved from the REST router to ingress, which made the H9.7 invariant STRONGER rather than merely different: the old rewrite sat inside `rest.js`, so the router -- and everything `app.js` routes outside it, including the picture -- could see the prefix, which is exactly why the picture was unreachable. Door-blind code is now everything but the four lines that strip. `AGENT_DOOR` is an explicit two-entry table, NOT a blanket strip, because stripping to anything would put every route `app.js` later grows behind the IAP-free backend the moment somebody added one, with nobody deciding to. No estate change: the URL map already matched `/connect/*` with a wildcard. Three mutants bite, including the blanket strip and the door applied after identity. |
| **B102** | **An agent that loses its lock token cannot discover when the lock frees, and the field that looks like it would say is always null.** `expiresAt` is returned once, by `POST .../lock`, and appears nowhere else. `GET .../lock` answers `owner` and `heldUntil`, and `heldUntil` is the D22 post-reclaim HUMAN hold rather than the lock's own expiry, so it reads null throughout a perfectly ordinary agent-held lock. An agent that crashes, restarts, or simply fails to capture the response is locked out of its own diagram for the full 60 second TTL with no supported way to learn that number -- it must poll and infer. Found by doing it: the token was printed truncated and discarded, and the only way forward was to wait blind. | `[V, server/locks.js:49 acquire returns expiresAt; server/locks.js:91 heldUntil reads this.holds, the D22 hold, not this.map; server/rest.js:371 GET lock returns owner and heldUntil only; observed live: GET /connect/v1/diagrams/<id>/lock answered owner server, heldUntil null while the lock was held]` | FIXED -- H9.32. `GET .../lock` now carries `expiresAt` beside `heldUntil`, and API.md states that they are two different waits rather than leaving the reader to infer it: `expiresAt` is when the holder's lock lapses on its own, `heldUntil` is the D22 post-reclaim hold that stops an agent taking a lock nobody holds. The accessor reads a LIVE lock rather than the map, which matters because a lapsed lock sits in the map until the sweep runs -- reading the map would have worked in every test that releases cleanly and reported a stale instant exactly when an agent is waiting on it, which is the only situation the field exists for. Two mutants bite, and the second only bites because of a fake-clock test; the REST test alone did not catch it. |
| **B103** | **A rejected commit tells an agent what was wrong and not which operation was wrong, although the server computed it.** `plan()` returns `at: i` naming the failing index, and the REST layer passes only `result.error`, so the index is discarded at the boundary. A twenty-two operation batch refused with *invalid value for node.x* leaves the caller bisecting by hand. Two rejections during the dogfood happened to name the offending id, which is why this looked survivable; an error that names a FIELD rather than an id would not have been. | `[V, server/txn.mjs:77 returns { ok: false, error: step.error, at: i }; server/rest.js:182 returns { error: result.error } and drops at; tests/txn.test.js asserts the index exists at the store boundary, so nothing below REST is at fault]` | FIXED -- H9.33. The 422 now carries `opIndex`. Renamed from `at` on the way out: this codebase used `at` for a Change timestamp twelve lines below in the same file, and for a region offset array in `commands.js`, so three meanings shared one name and a search on it could not be resolved by reading the hit. The rejection index was the only one of the three no client had seen, so it was the one still free to move. `-1` means the request was refused whole rather than at an op. Two mutants bite. |
| **B104** | **The entity id format an agent must produce is enforced everywhere and stated nowhere it would be read.** Ids are `<kind>-<six lowercase hex>` and the validator refuses anything else, but API.md -- the document written for whoever drives the API -- never says so. Two of the first three write attempts were refused for this alone, once for a plausible abbreviation (`wpt-`) and once for a readable name (`link-cross1`). The rule was learned by reading another diagram's data and guessing from it, which is the failure mode a reference document exists to prevent. | `[V, docs/spec/API.md contains no id grammar; server/validate.js enforces the per-kind id pattern; live refusals 'invalid id for waypoint: wpt-aa0001' and 'invalid id for link: link-cross1' during the H9.29 dogfood]` | FIXED -- H9.34. API.md gains a **What an id looks like** section carrying the enforced pattern, the six kinds, and the narrower selectable subset. Held by a test rather than by care: it greps the live regex out of `validate.js` and asserts API.md contains it byte for byte, then runs every example id in the doc through it. A copied regex is a twin and drifts silently -- validate.js gains a kind, the doc keeps the old list, and an agent written from the doc is refused for a reason the doc denies. Two mutants bite. |
| **B105** | **An agent cannot direct a human's attention, so the collaboration is one-way and unobservable.** Every websocket verb is client-initiated -- `hello`, `open`, `resume` -- and the only things the server pushes are `change`, `selection` and `lock`, each scoped to the diagram a client is ALREADY viewing. An agent that creates or edits something therefore has no way to say *look at this*, and a human has no way to follow along without being told out of band which diagram to open and opening it by hand. Found by the director asking to watch an agent work and seeing nothing: the request was to switch the open view to the agent's diagram and draw there, and no mechanism for the first half exists. This is the other half of B100 and the pair is what H9 means by agent-PLUS-human: B100 is the human cannot reach what the agent made, this is the agent cannot point at it. Note what is NOT broken, so nobody looks there: fan-out works, and had the agent drawn on a diagram the human already had open -- which its workspace grant permitted the entire time -- every edit would have appeared live. | `[V, server/protocol.js dispatches hello, open, create, commit, undo, redo, resume, select, reclaim, delete, list -- all client-initiated; the only server pushes are broadcast(id,'change'), (id,'selection') and (id,'lock') at server/rest.js:191, :212, :484, :495, :596 and server/protocol.js:368, every one scoped to a diagram id; no verb or event moves a client's view]` | FIXED -- H9.35. A second indicator beside the pill, reporting agent activity across the WORKSPACE. Server: a lock records who holds it and when; `locks.activity()` derives the list from the LIVE locks so it cannot disagree with them and an agent that dies holding one stops being reported without anything noticing it died; `hub.announce` fans out to every session whatever it is viewing, because `broadcast` is per-diagram and the whole value here is reaching the operator who is NOT looking at that diagram. The list rides the snapshot, an `agents` announcement, and `GET /api/v1/workspace/agents`, all three from one call. Client holds it as state and REPLACES on announcement -- the server sends the whole live set, and merging could only invent an entry it never reported. The indicator names the diagram rather than its id, since an id names something the operator cannot picture, and clicking opens it; clicking is suppressed when the work is already in view, because a control that goes nowhere is worse than none. Hidden entirely when the workspace is quiet. Eight mutants bite. Two survived the REST tests -- the principal not being recorded, and a lapsed lock still reported -- because authorization is off there and no fake clock existed. CONFIRMED live by the director on `draw-00024-qft`: `planner is driving` on the diagram in view, the diagram NAME when the work is elsewhere, and the pill unchanged throughout -- which is the co-occurrence property the ruling turned on, observed rather than argued. |
| **B106** | **Every change written by anyone else is silently discarded by a watching browser, so live collaboration does not work at all.** Two lines in `onMessage`: `setCounts` advances the local version to the POST-change value, and `applyChange` then asks whether the change is stale by comparing `body.from` against that same version. The guard is `if (body.from < v) return` and its intent is right -- already applied, never re-apply -- but its input was corrupted immediately before it ran, so `22 < 23` is true for every change and none is ever applied. No resync fires either, because that is the opposite branch. Reopening the diagram works because a snapshot bypasses the guard, which is exactly what the director observed: nothing appeared live, switching away and back showed everything. This is not an agent defect -- two humans in two tabs have the same experience, and it means the collaborative editor has never propagated an edit in a browser. Registered first as an observation with the cause deliberately not guessed -- that row and this one were both filed as B106, and are merged here rather than left as a duplicate id in the ledger. The original observation stands as written: An agent's writes did not appear in a browser watching the same diagram, and the lock indicator never cleared, although every write was durable and every broadcast was sent.** Observed live, not in a test: the agent took the lock on `a1-demo`, the director's editor correctly showed read-only, the agent committed eight transactions taking the diagram from v22 to v30, released the lock, and the browser showed neither the drawing nor the release -- it was still displaying LOCKED after the server had freed it. So the client received the FIRST broadcast and applied nothing afterwards. Ruled out by inspection: stale client code, since static assets are served no-store; the renderer being suppressed while read-only, since it has no knowledge of that state; the commit route failing to broadcast, since `commitWrite` calls it on the same hub that delivered the lock event; and a dead socket, since the hub logs a named failure and none appeared. Cause unknown and deliberately not guessed. What makes this sharp is that GR6 asserts an agent write reaches both browsers AND PASSES -- against a test harness rather than a real client -- so the suite has been green over a path that does not work in a browser, which is the strongest argument yet that the harness is not the client. | `[V, app/src/sync.js onMessage for cmd change calls this.changes.setCounts({ version: b.version }) and then this.applyChange(b); applyChange reads const v = this.changes.state.version and returns when body.from < v; app/src/changes.js:133 setCounts assigns state.version; server/protocol.js:129 changeBody always sends version, so the field is never absent in production. Observed live on a1-demo: eight commits v22 to v30 durable and broadcast, none rendered, all present after switching diagrams and back]` | FIXED -- H9.36. `sync.js` now carries `appliedVersion`, the version the MODEL is at, distinct from `changes.state.version`, which is the server version shown in the UI counter and is set the moment a message arrives. `applyChange` reads and advances the new field; it is also set from a snapshot, from a `resume` that found us in step, and from our own `ack`. Conflating the two was the whole defect. Proven by reverting the client under the repaired harness: 4 of 5 GR6 tests red before, all green after. Four mutants bite -- one per site that advances it. Deployed as `draw-00017-4tz` and the three-command verification passes. CONFIRMED live by the director in both halves. Two tabs on one diagram: a write in one appears in the other with no reload and no switching away. Then the original scenario re-run exactly -- agent takes the lock on `a1-demo`, the editor goes read-only, nine commits take it v71 to v80, the lock releases and the indicator clears. The nine deliberately spanned op kinds, because the first report was almost all creates and a `set` that failed to render would have been a different defect hiding under this one: a two-link single transaction arriving as one step, a node move, a zone resize and a rename all landed live. |
| **B107** | **The convergence harness reimplements the client's inbound-change rule instead of calling it, and its copy is correct while the real one is broken.** `applyTo` in the test file carries `if (body.from < q.version - 1) return` where `sync.js#applyChange` carries `if (body.from < v) return`. The minus one is the whole difference: the harness compensates for the version having advanced, and the client does not. GR6 therefore asserts *an agent write reaches both browsers*, passes, and exercises none of the code that would have to work for that sentence to be true. The suite has been green across a path that has never functioned in a browser, and the defect was found by a human looking at a screen. This is the session's recurring failure in its purest form: a check whose subject is a copy of the thing it claims to verify. Every other instance was a scanner reporting on a narrower set than its sentence claimed; this one reports on a DIFFERENT IMPLEMENTATION and cannot fail when the real one breaks. | `[V, tests/convergence.test.js applyTo(q, body) contains the guard with q.version - 1 and calls applyOps directly, never constructing a Sync or invoking onMessage; app/src/sync.js applyChange contains the guard without the adjustment; GR6 passes on the current tree while the live editor does not propagate a change]` | FIXED -- H9.37. Each peer now runs a real `Sync` over a faked transport and inbound messages go in through `onMessage`; `applyTo` is three lines that hand the message to the client. The harness also feeds the writer its own `ack`, which is what the server does and what tells a client its own model has advanced. Repairing it immediately turned GR6 red against the shipped client, which is the proof the old harness was testing itself: same tests, same assertions, real subject. |
| **B108** | **No check can see a test that reimplements its subject instead of calling it, which is how B107 survived every scanner in the suite.** `scan-twins` compares 245 functions for shared arithmetic and passed clean across the whole life of B107, because the reimplementation was four lines and differed by one term -- below any similarity threshold, and not arithmetic. `scan-dead` could not help either: `applyChange` HAS a production consumer, so it is live by that rule while no test reaches it. The defect class is a test whose subject is a copy, and it is invisible to every scanner because each one reasons about production code or about similarity, and this is neither: it is about which of two implementations a test is wired to. It was found by the director looking at a screen. Worth stating plainly that the suite currently has no way to answer "is this test exercising the shipped path", and that GR6 was green over dead code for as long as it has existed. | `[V, npm run gate on the pre-fix tree: scan-twins 245 functions compared, 0 pairs >=25%, PASS, while tests/convergence.test.js#applyTo duplicated app/src/sync.js#applyChange; scan-dead reports applyChange live via its onMessage caller; 550 tests green over a client that never applied a remote change]` | OPEN. Candidate rule: for a named production function that a spec sentence depends on, assert that some test reaches it -- coverage-shaped rather than similarity-shaped. Cheaper interim: when a harness models a client, require it to construct the real one, which is now true of the only harness that did not. |
| **B109** | **A deleted diagram is recoverable for seven days and nothing in the product says so, so in practice it is not recoverable.** The bucket carries a GCS soft-delete policy with 604800s retention, which is what makes `DELETE /api/v1/diagrams/<id>` an acceptable verb to add at all -- the director ruled B32 on the grounds that an agent can already delete every entity, and that reasoning does not survive on its own, because entity deletion is undoable in-app and instant while deleting the container destroys the undo log with it. The soft-delete window is the real backstop and it is invisible: recovering needs `gcloud`, knowledge that the policy exists, and noticing inside a week. | `[V, gcloud storage buckets describe gs://diagrams.apnex.io returns soft_delete_policy.retentionDurationSeconds 604800 and no versioning; server/store.js has no restore path; the word soft-delete appears nowhere in docs/ or app/]` | DEFERRED. Revival trigger: the first diagram deleted by accident, OR `DELETE` shipping to a second user. Wanted shape from the director: a recoverable-diagrams dialogue in the app listing what is inside the window, so the backstop is reachable by the person who needs it rather than by whoever has cloud console. |
| **B110** | **Grid snapping is a client behaviour, not a rule, so every write through the agent door lands wherever it likes and the engine's cell-parity guarantee is silently false.** The director noticed that the nodes an agent drew sit in invalid grid cells and asked whether the rules engine had failed to snap them. It had not: there is no rule to fail. `snapNode`/`snapZone` live in `app/src/snap.js` and run in the browser before a commit is sent; `server/validate.js` checks only that a coordinate is a number inside the surface bounds, and never that it is on the pitch. An agent writing over REST therefore bypasses snapping entirely and nothing anywhere reports it. This is not cosmetic. `engine/relations.mjs` states the parity guarantee as *cell-equality is px-equality on GRID OPERANDS*, and `cellOf` is `Math.round(v / 60)`, so off-grid operands break the precondition: `cellOf(-270)` and `cellOf(-240)` are both cell `-4`. Two entities at different pixels then occupy one cell in the R13 occupancy index, so `atCell` answers with the wrong entity and any rule resting on one-node-per-cell is wrong. The estate is contained today -- 8 off-grid entities in one diagram, zero actual clashes -- but a human dragging a node onto an adjacent on-grid cell would collide with an off-grid neighbour and neither would be told. | `[V, app/src/snap.js:19 snapTo with GAP=STD.pitch=60 is the only snapping in the tree and is client-side; server/validate.js:70-78 bounds x/y with num(v,-EXT,EXT) and has no pitch check; kernel/geometry.mjs:18 cellOf = Math.round(v/pitch); engine/relations.mjs:12 states the parity guarantee is conditional on grid operands; diagram-f35a46 has 8 of 9 nodes off-grid, e.g. fw-a at y=-270 where -270/60 = -4.5; waypoint-aa0001 was written off-grid at (-90,-150) by the agent and became (-120,-120) the moment the director dragged it in a browser -- one document, two paths, two rules]` | FIXED -- ruled 2026-08-23 as REFUSE, not snap: this is an engineer's tool, so geometry is a constraint rather than an aesthetic preference, and a refusal teaches the rule once where a silent move would leave an agent believing it drew something it did not. `server/validate.js` gains `onGrid`, applied to node/waypoint x,y (pitch), zone x,y (half-pitch offset) and zone w,h (whole cells). The PITCH is sourced from `STD.pitch`, the CHECK stays local -- the same split the surface extents already use. Order mattered: `validateDoc` runs on LOAD, so `diagram-f35a46` was migrated FIRST, through the API rather than by editing the bucket, or the store would have refused to boot on a document it had already accepted. The estate scan before shipping found 8 off-grid nodes and 1 off-grid zone, all in that one diagram; every human-made entity across 146 was already clean, which is itself the proof the client always snapped. Four mutants bite, including a zone validated against the NODE grid -- the exact mistake the author made by hand while migrating, caught by the test rather than by care. 19 test fixtures used off-grid coordinates and were corrected; that the suite had 139 such literals and never noticed is the same finding as the defect. |
| **B111** | **The zone grid has no owner, and B110 gave it a second implementation instead of one.** The kernel knows exactly one grid: `cellOf` is `Math.round(v / pitch)` and `cellPx` is `cx * pitch`, both offset 0, which is the NODE grid. The half-pitch offset that zones use is not in the kernel at all -- it lived only in `app/src/snap.js#snapZone`, and B110 added a second copy in `server/validate.js` as `HALF_PITCH`. Two implementations of one rule, in the same session that fixed B107 for being exactly that. `scan-twins` passes over both because each is a few characters, which is the same reason it passed over B107. The consequence is live rather than theoretical: an agent asking the kernel where a zone may sit gets an answer from the node grid, because the kernel has no other, and the author of B110 made that precise mistake by hand while computing a migration an hour earlier. | `[V, kernel/geometry.mjs:18 cellOf = Math.round(v/V.pitch) and cellPx = ([cx,cy],V) => [cx*V.pitch, cy*V.pitch], neither taking an offset; app/src/snap.js:33 snapZone passes HALF; server/validate.js:55 declares HALF_PITCH and :117 applies it; scan-twins reports 0 pairs >=25% across 245 functions with both present]` | FIXED. `kernel/geometry.mjs` gains `LAYOUTS` -- `node` at offset 0, `zone` at half a pitch -- plus `onLayout`, `snapLayout`, `cellOn`, `pxOn`, `nearestAnchor` and `anchorAt`. `app/src/snap.js` and `server/validate.js` both source a layout instead of restating the offset; snap.js keeps only the canvas CLAMP, which is a UI concern where the pitch is not. `nearestAnchor` answers the question the director asked for and carries BOTH representations, so no consumer multiplies or divides -- the two arithmetic steps that produced every off-grid entity. The layout name travels with an anchor because the same cell index resolves to different pixels on the two grids. Three mutants bite. |
| **B112** | **An anchor may hold two nodes, and the rules engine that should say so was never consulted.** `model/invariants.mjs` exists precisely for document properties no single entity can be asked about, and it opens by recording this exact failure: putting *one straight link per pair* in the two authoring sites left `set` unbound by it, so a rule enforced at call sites is a convention (B81). Occupancy is that shape again -- `Model#put` accepts two nodes at identical coordinates without complaint, and the R13 index keys occupancy by cell, so the index already assumes what nothing enforces. The live estate holds zero collisions across 146 entities, and only because a human dragging a node can see the one already there; the agent door has no such check and B110 stopped it writing off-grid without stopping it writing on top of something. Recorded also because the author wrote a design proposing this as a NEW rule while the engine for it already existed and went unmentioned -- the director had to point at it. | `[V, model/index.mjs Model.put has no occupancy check: putting node-aa0001 and node-aa0002 both at (60,60) yields 2 nodes; model/invariants.mjs#violations covers group double-membership and straight-link capacity and has no occupancy rule; engine/relations.mjs:9 keys cell occupancy as an eager R13 index; estate scan 2026-08-23 reports 0 colliding cells across 13 diagrams]` | FIXED. Four lines in `violations()`: every node and waypoint keyed by resolved coordinate, a second occupant reported. A waypoint counts because a waypoint IS a node for placement (ruled 2026-08-23). Compared as px rather than cell indices, so the rule states the property itself rather than depending on B110 holding. In the rules engine and not `validate.js`, which buys two things the planner relies on: the check runs against the state a transaction would PRODUCE, so a batch may transiently collide and end valid, and only what a transaction INTRODUCES is refused, so a document already holding a collision can still be repaired -- asserted directly. GR5 carries a FIFTH declared divergence, following B81. Three mutants bite. The corpus pass was the work, as predicted: fixture helpers defaulted to the origin, ring tests put hundreds of nodes at (0,0), and several files share one seeded diagram whose row y=0 fixtures kept landing on. A blanket coordinate rewrite broke tests that ASSERT positions and was reverted in favour of per-collision fixes -- the second time today a mechanical edit to fixtures was the wrong instrument. |
| **B113** | **The 2000-entity collection cap is no longer the limit that binds, and for positioned kinds it is now unreachable.** B112 caps nodes and waypoints at one per anchor, and the canvas holds 31 x 17 = 527 node anchors, so `validate.js`'s `exceeds entity limit` can never fire for them -- occupancy refuses at 528. The cap remains reachable only for kinds carrying no coordinates, and reaching it on links needs 2001 DISTINCT pairs because the straight-link invariant allows one per pair. The existing cap test filled 2000 nodes at a single point, which is exactly the document B112 forbids; it now asserts the anchor limit and states plainly that the cap is not asserted, rather than quietly testing something weaker. | `[V, server/validate.js:280 `if (list.length > 2000) return ...`; model/surface.mjs NODE_EXT {x:900,y:480} with pitch 60 gives 31x17 = 527 anchors; tests/server.test.js `collection caps are enforced on apply` fills every free anchor and observes refusal at capacity]` | FIXED, both halves ruled 2026-08-23. (1) The cap is DERIVED for a positioned kind -- `node` and `waypoint` are the node-anchor count (527), `zone` the zone-anchor count, `link` and `group` stay 2000 where the number is still reachable. (2) The cap has its own test on links, which needs 2001 DISTINCT pairs because the straight-link invariant allows one per pair, so it is a separate setup rather than a tail on the anchor test. Chasing this surfaced a THIRD statement of the limit: `server/txn.mjs` refused at 2000 on apply while `validate.js` refused at 2000 on load. Two enforcement points is right and deliberate -- one guards the wire, one guards what loads -- but two NUMBERS is not, and they diverged the instant one became derived. Both now source `engine/policy.mjs#collectionCap`, which already declared itself the authority for a threshold under B85. It also surfaced B110's remaining half: `validate` bounded by the SURFACE half-extent (960x540) while `snap.js` clamped a node to NODE_EXT (900x480), so a hundred positions were legal to the server and unreachable in the editor -- the same defect shape with the agent door as the unbound caller. Tightened to NODE_EXT for nodes and waypoints and ZONE_EXT for zones; the estate's worst case was |x| 840, |y| 480, so it cost nothing. Five mutants bite. |
| **B114** | **An agent cannot see which diagram a human is looking at, so it cannot be considerate and cannot follow.** The director asked whether the API could report their active diagram during the B105 walkthrough, and it cannot. The Hub already holds `session.diagramId` for every live session -- it is how `broadcast` scopes a fan-out -- so the fact is present and simply never read: no route exposes viewers, and `snapshotBody` reports locks, grants and version but never who else is here. This is the exact MIRROR of B105. That row gave the human a view of what agents are doing; nothing gives an agent a view of what the human is doing, so the reciprocity ACCESS.md required for ACCESS has no counterpart for ATTENTION. The consequences are concrete rather than aesthetic: an agent takes a lock without knowing it is about to make a human read-only mid-thought, it cannot choose to work somewhere the human is not, and a follow-mode -- the third B105 shape the director floated and did not pick -- cannot be built at all without it. | `[V, server/hub.js Hub holds a Set of sessions each carrying .diagramId and exposes only broadcast/announce; no route in server/rest.js answers viewers or presence; server/protocol.js#snapshotBody carries doc, diagrams, locked, mayWrite, principal, version, undoTop and agents, and nothing about other sessions]` | FIXED -- ruled 2026-08-23: a human CANNOT decline to be observed, because the control surface is the grant and the connection code rather than the visibility. Declining to mint is how you decline to be watched, and an agent already authorised may see where you are working. `Hub#viewers()` derives presence from the live sessions, so it cannot disagree with the registry and a session that drops stops being reported without anything noticing it dropped -- the same property `locks.activity()` has. Carried on the same three channels as `agents`: the snapshot, a `viewers` announcement, and `GET /api/v1/workspace/viewers`. Announced from one funnel, `announceActivity`, called wherever presence can change -- a session opening a diagram, a session closing, a lock moving. Three mutants bite. |
| **B115** | **The agent indicator never clears when a lock lapses, so it reports work that stopped.** Observed live by the director during the B105 walkthrough: the lock on `agent-dogfood` expired by TTL and the indicator kept showing it. `hub.announce('agents')` fires on explicit acquire and release only. A lock that simply times out is announced to nobody, so the PUSHED copy goes stale while `GET /workspace/agents` would answer correctly -- the pull is right and the push is wrong, which is worse than both being wrong because it looks like it works. The mechanism to fix it already existed and went unused: `sweep()` is documented as the sole expiry-deleter precisely *so it can always report the freed diagram to viewers*, and `app.js` already calls it every 5s to broadcast a per-diagram `lock`. B105 added a second thing that needed telling and told only the first. Sharper because the B105 commit CLAIMS this works -- it says an agent that dies holding a lock stops being reported without anything having to notice it died. True of `activity()`, false of every client already connected, and no test caught the difference because every test released explicitly. | `[V, server/app.js:343 sweep().forEach broadcasts lock and does not announce agents; server/rest.js announces agents on POST and DELETE of a lock only; server/locks.js#_live comments that sweep is the sole expiry-deleter so it can report the freed diagram; observed live on draw-00024-qft: lock on diagram-f35a46 lapsed and the indicator remained]` | FIXED. `sweepLocks(locks, hub)` in `server/app.js` now announces the agent list whenever a sweep frees anything, once for the sweep rather than per diagram -- the body is the whole live set, so a second send repeats the first. Extracted from the arrow inside `setInterval` and given a name specifically so it could be tested: every test in the suite released its lock explicitly, so expiry was the one path nothing reached, which is exactly the gap the defect shipped through. Recorded in `scan-dead`'s ALLOW because its only production caller is the timer in the same file -- the same shape as `iapIdentity`, and inlining it again would retire the test that proves a timed-out lock clears the indicator. Two mutants bite, one of them the pre-fix behaviour. CONFIRMED live by the director on `draw-00025-87b`: a lock abandoned without release -- what a crashed agent leaves behind -- cleared the indicator on its own within the sweep interval, with nothing but the sweep telling anyone. |
| **B116** | **The agent list is filtered when pulled and unfiltered when pushed, so a session learns about diagrams it may not read.** `GET /api/v1/workspace/agents` filters `locks.activity()` by `canRead`; every `hub.announce('agents')` sends the whole list to every connected session. With authorization on, a principal with a grant on one diagram receives the ids of every diagram any agent is driving, and the name of the agent driving each. The document stays private and its EXISTENCE does not, which is the distinction ACCESS.md draws when it says `list` is the surface that leaks what exists rather than what it says. Introduced by B105 in this session, and it is the same pull-right-push-wrong shape as B115 one row earlier -- found by reading the two call sites next to each other rather than by any test, because every test runs with authorization off, where `canRead` is true for everyone and the two paths agree. | `[V, server/rest.js:286 filters agents by store.canRead(a.diagram, principal); server/rest.js:580,600,611 and server/app.js:163 and server/protocol.js:378 all call hub.announce with the unfiltered list; server/hub.js#announce sends one body to every session by construction, so no call site could filter it]` | FIXED with B114. `Hub#announceEach(cmd, build)` sends a body built PER SESSION, which is the same shape `retarget` already needed and for the same reason -- what a session may be shown depends on who it is, so one body for many sessions cannot be right for any surface carrying identity. |
| **B117** | **The CLI cannot reach the deployment, cannot authenticate, and cannot write -- and the agent it was built for went a whole day without reaching for it once.** `cli/README.md` calls it *the first external program to live entirely on the API contract* and says it is *built for agentic interrogation as much as for humans*. It speaks only `/api/v1`, which is behind IAP in production, and `draw.sh diagrams --host https://draw.apnex.io` answers `invalid (non-JSON) response` -- it is receiving a Google sign-in page. `grep` for Authorization, Bearer or connect in all 356 lines returns nothing: there is no auth of any kind, so it cannot use a connection code and cannot open the agent door. Its stated design premise is also dead. *Model mutations stay with the browser (single-writer rule)* was true when it was written and was overturned by H9, which built commit, lock, undo, create and now delete on `/connect/v1` precisely so an agent could write without a browser. The evidence is behavioural rather than theoretical: across a full session of driving the live estate -- creating diagrams, committing transactions, taking locks, reading anchors, deleting -- the agent used `curl` and `python3` every time and never once considered the tool written for it. It could not have used it. | `[V, cli/draw.sh has 356 lines and no occurrence of Authorization, Bearer, connect or any token; every request targets ${APIHOST}/api/v1/...; cli/README.md states read-only by design and that mutations stay with the browser; bash cli/draw.sh diagrams --host https://draw.apnex.io returns `invalid (non-JSON) response`, which is IAP's HTML sign-in]` | OPEN. Not a small fix and not obviously worth doing as-is, which is the question: the CLI predates the agent surface and an agent now drives that surface directly over HTTP without wanting a shell wrapper. Three honest options -- teach it `/connect` plus a bearer code and the write verbs; narrow its README to say local-and-read-only so it stops claiming a role it cannot fill; or retire it under W3 with a disposition for the jq templates. A ruling is wanted before any of them. |
| **B118** | **GR10 derives the route inventory with a pattern that cannot see two of the shapes the router uses, so API.md coverage is checked over a subset.** It matches `parts[n] === 'x'` only. `server/rest.js` also routes on a literal `url.pathname === '/health'` and on the NEGATIVE `parts[2] !== 'diagrams'` that guards the entire diagram family -- so the two most-used surfaces in the product are the two the check cannot see. Found while writing `scan-cli.mjs`, which inherited the derivation and reported the CLI reaching 1 route of 21 when it plainly reached three; the arithmetic was wrong in a way that was visible, which is the only reason it surfaced. GR10 has been green throughout, and its header claims every REST path the server answers is documented. B96 already widened this check once, from one path POSITION to any position, and the same sentence was left over-claiming about path FORM. | `[V, tests/spec.test.js GR10 matches /parts\[\d+\] === '([a-z0-9]+)'/g only; server/rest.js:257 `if (url.pathname === '/health')` and :318 `if (parts[1] !== 'v1' || parts[2] !== 'diagrams')`; tools/scan-cli.mjs now matches `[!=]==`, the pathname literal, and drops the `v1`/`api` prefixes]` | FIXED. `tools/routes.mjs` is the one derivation, consumed by GR10 and by `scan-cli`. It sees all three shapes the router routes on -- a positive `parts[n] === `, the NEGATIVE `parts[2] !== 'diagrams'` that guards the diagram family, and a literal `url.pathname === '/health'` -- and drops the `v1`/`api`/`connect` prefixes, which name a version and a family rather than an operation. A second test guards the derivation itself, asserting each shape is still seen: if it narrows again the coverage it feeds narrows silently, and both consumers would pass over whatever it stopped seeing. Widening it immediately found a real gap: `/health` has been served since it was written and API.md never documented it, which is precisely the class GR10 claimed to hold. Now documented. |
| **B119** | **GR18 coverage is per path SEGMENT, so a read verb marks a whole route family covered and the write verbs on it stay missing.** `draw link path` reaches `GET /links/<id>/path`, and `scan-cli` therefore counts `links` reached while `POST /links` -- the high-level create verb CLI.md specifies -- does not exist and nothing reports it. Same for `zones`. The check is a floor rather than the claim its name suggests: it proves no route family is entirely unreachable, not that every method on one is driven. Caught while landing the contextual verbs, because two PENDING lines went stale for the wrong reason -- the countdown moved without the work being done, which is the failure mode the stale-entry check was written to prevent and did not. | `[V, tools/scan-cli.mjs derives segments from each verb`s declared route and from parts[n] comparisons in server/rest.js, neither carrying a method; cli/verbs.mjs `link path` and `zone contents` are GET-only and marked links/zones reached]` | FIXED. `server/routes.mjs` DECLARES the surface -- 32 routes, 42 route+method pairs -- because methods could not be inferred from the router honestly, and two attempts at inferring them are recorded in `tools/routes.mjs`. A declaration nothing checks is a wish, so `tests/routes.test.js` issues every pair against a running server and fails on 404 or 405; any other status means the route exists and the request reached its handler, which is all that is claimed. The reverse direction is held by `tools/routes.mjs`, which derives the families independently, so a route added to the router and forgotten in the declaration is still caught. Three mutants bite: a declared route that is gone, a method the router does not take, and a live family left undeclared. `scan-cli` now compares route AND method, and a verb declares `also` for the extra pairs a composite reaches -- `place` reads the document and the free anchors before it commits, so declaring only the commit would have marked those reads unreached. Sharpening the check immediately found five gaps the family-level version hid: two verbs declaring the collection rather than the path they hit (`revoke`, `code revoke`), and three verbs CLI.md specified that were never built (`layouts`, `workspace grants`, `workspace revoke`). It also caught `place` and `add` declaring GET while issuing POST, which the auto-derivation had got wrong. |
| **B120** | **A tool error recommends a verb the tool does not have.** `draw add server at 130,60` answers *those look like pixels; `draw anchor nearest 130 60` converts them*, and there is no `anchor` verb. Worse than a vague message: a reader who trusts it spends time discovering the tool lied, and then has reason to doubt every other message it prints -- including the ones that are right. The class is wider than this instance. Nothing checks that a verb named inside an error string exists, so any refactor that renames or drops a verb leaves working code pointing at a ghost, and only a human running the failing path would ever find out. | `[V, cli/verbs.mjs `add` dies with a message naming `draw anchor nearest`; `node cli/draw.mjs help` lists no anchor verb; tools/scan-cli.mjs still carries `nearest` and `anchors` as PENDING, so the coverage countdown already knew the routes were unreached while the error text assumed otherwise]` | FIXED by building `anchor nearest` and `anchor free`, which were pending anyway. The general check -- every verb named in an error string is a verb that exists -- is the durable part and is added as a test. |
